# 更新日志

## 2.0.0-rc.35（2026-08-21，测试仓候选）
- rc.34 当前指纹在真实 TauriTavern 同一全新聊天中完成 2 个有效回复；第3个正文和选项正常，人物 `no_candidates`、世界 `world.committed`，但严格变量通道两次响应均成功返回后，同一现有普通对象下两个新成员仍被宿主 MVU 静默丢弃，最终 `variable.final.failed`，Doctor 总耗时 `102386 ms`。按硬门停止于第3回合，未 reroll、未发送第4回合，rc.34 不能继续验收。
- 根因是既有本地修复只把缺失叶子合并为最近动态父对象；真实 MVU 同时静默丢弃该父对象的 replace 时，代码没有继续尝试仍然存在的普通对象祖先。rc.35 在同一解析/事务链中逐级尝试祖先级机械合并；每一级都从同一 expected state 重建，仍须通过 MVU parser、Schema、触碰路径和所有未触碰字段保留校验。数组、只读、混合失败、缺失祖先或无关字段损失继续零写入。
- 变量/accepted-final 直接相关定向测试 64/64 与本候选唯一一次完整自动套件 669/669 均通过，0 fail，duration `13537.8621 ms`。运行代码变化后，rc.34 的两轮真实成功和第3轮失败只作根因证据；须推送并加载 rc.35 当前指纹，再从一个全新聊天重新执行十二回合与恢复门禁。

## 2.0.0-rc.34（2026-08-21，测试仓候选）
- rc.33 真实酒馆第1个 accepted 回复完成变量、`no_candidates` 人物终态、P3 durable commit 和 P4；第2个 accepted 回复正文、选项和 P3 正常，但真实隐藏人物档案块进入恢复持久化时抛出 `actorProfileRecoverySourceDigest is not defined`。根因是成熟 V6 recovery helper 仍是模块私有函数，生产语义适配器却直接引用它；旧合成夹具没有执行这个真实失败人物目标分支。
- 当前候选显式导出并导入同一个 recovery SourceRef digest helper，不复制第二份摘要算法；同时把语义 P1 的恢复持久化、终态诊断和状态展示收进兜底边界，即使未来终态阶段再出现异常也必须返回红色 `not_completed`，不能留下 busy/waiting 或绿色假成功。人物完整度、`full_adult` 生理门、MVU 原子提交/readback、数据库独立性和 P3/P4 所有权均未放宽。
- 人物/accepted-final 定向检查 113/113、整份 ES module 解析 1/1 以及本候选唯一一次完整自动套件 668/668 均通过，0 fail，duration `13905.969 ms`。rc.33 两个真实回复只作根因证据，运行代码变化后不能续接 rc.34；须重新加载当前指纹并从一个全新聊天重新执行十二回合与恢复门禁。

## 2.0.0-rc.33（2026-08-21，测试仓候选）
- rc.32 首个真实酒馆 accepted 回复证明正文、变量事务和 `no_candidates` 人物终态正常，但 P3 在任何世界模型调用前以 `world.unexpected_failure` 终止，内置“修复世界连续性”也因复走同一路径返回 `doctor.repair.world.adapter_failed`。根因是生产 `runContinuityTarget()` 在本地 Recall 前引用未声明的 `recalledActorIds`，而 VM 夹具曾手工注入同名全局并遮住浏览器 `ReferenceError`。
- 当前候选从本回合冻结的 `scheduledActorIds` 与已持久化待裁决尝试构造唯一 `mustActorIds`，同一集合同时用于关系目标和本地 Recall；测试夹具不再补生产全局，并新增未声明引用回归。定向检查 158/158、唯一一次完整自动套件 666/666 通过，0 fail，duration `13520.7757 ms`。此修复不改变人物档案、MVU、数据库、预设或 P4 的权威边界。rc.32 的真实失败证据不能续接 rc.33，必须重新加载当前指纹并从一个全新聊天重新执行十二回合门禁。

## 2.0.0-rc.32（2026-08-21，测试仓候选）
- 真实宿主预检发现同名 IZUMI 配套预设仍载入旧内容；导入当前仓库文件后进一步证明 `Doctor_Profile_Context_Separation_V6` 虽存在于 `prompts`，却未进入 `prompt_order`，因此不会实际送入正文模型。当前候选把该条目唯一启用在人物语义块之前，同时保持 V5 回执终检为最后一项；合同明确 Doctor 先读取 accepted-final 原始 assistant 回执，后续正文模型只获得有界相关摘要且不重放历史隐藏档案块。
- 新增配套预设生产顺序回归，要求 V6 隔离条目存在、启用、只排序一次且早于人物语义块。更新后的精确预设 SHA-256 为 `CDFCCBA82EF9DBD8CFF627143C687F3E010876901CFD57981C29B1C70919B5D4`；129/129 定向检查和本候选唯一一次完整自动套件 665/665 通过，0 fail，duration `13485.5111 ms`。测试候选已推送 testing `main`，仍须从扩展管理器加载 rc.32 并完成同一全新聊天十二个有效回复及恢复门禁。

## 2.0.0-rc.31（2026-08-21，测试仓候选）
- 修复 rc.30 在真实 TauriTavern 中通过模块解析后、默认开场 hydration 暴露的初始化崩溃：`stripClosedProposals()` 的成熟接口直接返回字符串，生产 `stripMechanism()` 却仍按已退役对象形状读取 `.text`，随后 `.trim()` 抛错，使初始化标志已置位但 API/设置面板未挂载。当前接线直接消费字符串并保持原有 proposal 清理语义。
- 新增不含 `<content>` 包装的真实默认开场回归，要求生产 accepted-content 路径可直接归一化普通字符串。rc.31 唯一一次完整自动套件为 664/664，通过 0 项失败，duration `13546.2205 ms`；仍须完成测试仓推送、真实加载和十二回合门禁。

## 2.0.0-rc.30（2026-08-21，测试仓候选）
- 修复 rc.29 在真实 TauriTavern 更新并加载精确提交 `358ebe5` 后暴露的生产语法阻断：人物档案根缺失时的原子 replay 分支漏写条件表达式的空分支，浏览器模块解析以 `Unexpected token ')'` 终止，Doctor 因而完全未初始化。现补回显式 `: []`，不改变 MVU、人物完整度、SourceRef、P3/P4 或数据库所有权语义。
- 新增“整份生产 `index.js` 以 ES module 语法解析”回归，防止 VM 切片和普通 `node --check index.js` 在当前 Windows/Node 调用形状下漏掉真实浏览器解析错误。rc.30 唯一一次完整自动套件为 663/663，通过 0 项失败，duration `13601.2202 ms`；仍须完成测试仓推送、真实加载指纹和十二回合门禁。

## 2.0.0-rc.29（2026-08-20，测试仓候选）
- 完成 MVU durable semantic profile 前移与四项长期优化：accepted-final 原始 SourceRef 事务、assistant outgoing prompt 隔离、Doctor-owned 聊天清理、人物/势力/环境三 lane、有限 resolved 压缩和临时 actorOperationalState；数据库、预设、MVU、Doctor/P3/P4 所有权保持独立，语义档案不双写 legacy `profileV6`。
- prompt-ready 过滤按宿主实际静态事件负载接线，支持 string/content[]/四种回执包装并对 system/user 同文碰撞 fail-closed；CHAT_DELETED/GROUP_CHAT_DELETED 只接受已证明的 chat_file_name 位置参数，不能权威枚举 chat IDs 时不自动 GC。
- P3/P4 只召回当前相关人物和可感知结构后果；pending/坏人物不会阻塞 ready 人物或结构 lane。运行态按 MVU 可选 JSON Pointer 绑定、attemptId 与 world-adjudicated receipt 投影，缺绑定明确显示 unbound。
- 新增 projection recovery：当新 Actor 的 MVU 档案已持久化但 Registry/ledger 投影失败时，恢复链从当前 MVU 自然身份、稳定 ActorId、exact SourceRef 与 committed profile digest 重建候选；ledger 尚无该 Actor 不再永久阻塞，来源或档案篡改仍保留 receipt 并零写。
- 候选预设固定为 IZUMI 文件，V4 停用，V5/V6 采用唯一输出顺序，并要求消费票据但缺项的人物保留 ticketId/姓名/正文锚点/已有段供单人物修复；当前 SHA-256 为 `5A4996E917F653C5CED4C24E422419940A5B79EEB8CF45ED55802D379F26F487`。首轮完整自动套件为 658 项中 626 通过、32 项失败；修复旧 VM fixture 合同漂移与 resolved 重开 P1 后，经用户明确授权运行的最终完整自动套件为 662/662，通过 0 项失败，duration `13499.7558 ms`。真实宿主/模型或十二回合正式门禁尚未运行。

## 2.0.0-rc.28（2026-08-20，测试仓候选）
- 修复 rc.27 真实第二个 accepted 回复暴露的前端终态矛盾：MVU 卡片已经六段完整且 `atomic_readback`，但语义路径把曾经存在的恢复材料继续投影为 `canRetry`，导致顶部摘要错误变红并提示未绑定失败。成功终态现在明确清除可重试标记；前端也只在非成功终态采信 `canRetry`，真正的 `not_completed/failed` 仍保持红色。
- 修复同一真实回复暴露的 `full_adult` 假绿：语义编译器不再硬编码 `basic`，而是读取当前人物档案完成模式；启用成人生理档案时，复用 V6 成熟的六项生理覆盖校验，缺任一项整个人物零写/不 ready。短摘要、空的旧 physiology 模块和缺少本地合同版本都不会显示绿色；单人物修复只补该人物缺失生理项，再走同一 MVU CAS 与 durable readback。
- 配套最新版预设和生成前票据提示同步要求 `full_adult` 的六项自然中文生理语义；模型不写 revision、digest、SourceRef 或合同版本。runtime fingerprint 现在直接覆盖生理校验、ready 门和当前完成模式接线。
- 新增“完整绿色卡 + atomic_readback + 过期 canRetry”合成回归，要求摘要显示已保存且无需操作；原有零卡片持久失败仍须红色并保留修复中心入口。运行时代码变化使 rc.27 真实证据失效，rc.28 必须重新加载后在同一已接受正文上恢复并继续单聊天十二个有效回复。

## 2.0.0-rc.27（2026-08-20，测试仓候选）
- 修复 rc.26 同一测试聊天 swipe 19 的真实轻微格式偏差：当前 accepted assistant 只有一个隐藏人物回执，但主模型把它紧贴在 `</content>` 内侧；旧解析器只接受外侧插槽，因而正确零写却把可本地恢复的格式问题记为 `profile_block_missing`。解析器现在只额外接受“回执结束后仅有空白并立即关闭 `</content>`”的唯一内侧尾注释，并记录本地 relocation；正常 post-content 与旧聊天 true-EOF legacy tail 仍保留。
- 内侧兼容不扫描数据库、世界书或正文自由 JSON：仍只处理当前 accepted assistant 的专用注释，重复块、注释后继续正文、论坛/选项内伪块、无条目、错误 ticket/ActorId、六段缺失和精确 SourceRef 漂移继续 fail-closed；人物模型调用仍为 0，MVU CAS/readback、P3/P4 与前端权威不变。
- 新增完整人物块内侧尾、本地修复码、精确 no-change 内侧尾与“注释后仍有正文”拒绝回归；定向 262/262 与本候选唯一一次完整自动套件 616/616 均通过。当前仍须完成测试仓 main 推送、实际加载新指纹及同一聊天同一首回合 direct reroll；不代表十二回合正式门禁通过。

## 2.0.0-rc.26（2026-08-20，测试仓候选）
- 修复 rc.25 同聊天 swipe 7 的真实人物档案零写：模型交回的隐藏块实际为单新人物、同票唯一绑定、正文锚点命中且 V6 六段完整，parser/binder/compiler 与只读 MVU parse 全通过；失败来自 accepted-final 后宿主继续规范化 MVU/机制尾部，整条消息哈希变化触发旧写前门。新适配器要求 accepted 正文、专用人物块、swipe/generation/scope/epoch 与精确 MVU 基线连续稳定，再用规范化后的同一 SourceRef 进入现有 CAS/readback；正文或人物块变化仍零写。
- 修复 fresh chat `turn 0 → nextTurn 1` 的结构世界 safe-held 不可达：只把机械 turn 差异降回基线 turn，同时要求线程、world、scenario 与已裁决 ATT 均无语义增量；本地生成可验证 held 收据，不把“人物尝试”伪装成世界结果，也不要求第二次世界补缺模型才能保活。
- 新增人物事务尾部规范化/MVU 静稳与档案块漂移 fail-closed 回归，以及 fresh-chat 机械 nextTurn safe-held 回归；定向 153/153、accepted-final/parser 66/66 与本候选唯一一次完整自动套件 616/616 均通过。推送、重新加载及同一聊天同一首回合 direct reroll 仍待本候选完成；仍为未完成十二回合正式门禁的测试候选。

## 2.0.0-rc.25（2026-08-20，测试仓候选）
- 修复真实新聊天空 ActorRegistry 的 P3 永久 stale：P3 在比较只读账本投影时，用已经验证的当前 scope 补齐空/旧 Registry 缺失的 `scopeDigest`，不写人物、不接管 MVU，也不放宽真实聊天或作用域冲突。此前空账本在每次 fresh reacquire 都被误判为 `world.stale.actor_ledger_changed`，导致结构世界和 P4 无法进入终态。
- 新增真实故障形状回归：空账本、同一 accepted SourceRef、零 ready 人物时，结构世界比较必须继续，同时仍保持零人物调度、零行动收据。rc.24 同聊天 swipe 6 已证明 V5 回执顺序正确但暴露本缺陷；运行代码变化后该旧证据失效，rc.25 必须重新加载后在同一聊天同一用户消息继续 reroll。
- 定向 P3 100/100 与 accepted-final/P1→P3 53/53 通过；rc.25 唯一一次有效完整自动套件 614/614 通过（此前一次目录参数调用发现 0 个项目测试并以模块不存在退出，不计作有效套件）。语法、manifest/配套预设 JSON 与差异检查通过。仍为未完成正式门禁的测试候选；不把单次首回合、自动测试或旧指纹证据描述为十二回合正式验收。

## 2.0.0-rc.24（2026-08-20，测试仓候选）
- rc.23 在唯一测试聊天的同一首回合第四次直接 reroll 中，真实模型生成了正常正文和四个选项，并交回 `<!-- 人物档案无变化 -->`，但把回执放在 `<options>` 之后。根因是配套预设旧 `Four_Options_Output_Contract` 仍把 `</content> → <options>` 声明为“唯一顺序”，与新增的 `</content> → 人物回执 → <options>` 合同正面冲突；本地解析器正确以 `profile_block_missing` fail-closed，零档案、零 Registry、零人物模型调用，P3 stale 且无 world reserved/P4 污染。
- 配套预设新增终检 V5，作为最高优先级补丁明确取代遗漏人物回执的旧选项顺序：`</content>` 后下一个非空内容必须是且只能是完整人物档案更新或无变化回执，然后才允许 options、UpdateVariable 与其他收尾。V5 还要求提交前回看刚完成的正文；只要首次原创人物实际消费骰票，就不得错误选择“无变化”。
- Doctor 的生成前票据提示同步使用同一 V5 唯一顺序；新增 `ACTOR_PROFILE_PRESET_CONTRACT_VERSION` 并纳入 runtime fingerprint，避免外部预设合同与本地注入合同再次漂移。accepted-final 严格位置解析、MVU 原子提交、P3/P4、前端和 repair 边界均未放宽。
- 定向自动检查 275/275 覆盖真实冲突形状、V5 在 prompt order 的最终位置以及 preset-contract fingerprint 绑定；rc.24 唯一一次完整 Node 自动套件 613/613，并通过语法、manifest/配套预设 JSON、差异与敏感信息检查。当前仍须完成测试仓 main 推送、实际安装加载和同一聊天同一首回合继续 reroll 复验；十二回合正式门禁未完成。

## 2.0.0-rc.23（2026-08-20，测试仓候选）
- rc.23 首次真实加载核对发现扩展源码哈希与新 runtime fingerprint 已匹配当前提交，但普通诊断版本仍沿用 rc.22 常量。最终 rc.23 统一运行时与 manifest 版本，并新增二者精确一致回归，避免“代码已更新、用户仍看到旧版”的假缓存诊断。
- rc.22 在唯一测试聊天的同一首回合第三次直接 reroll 后得到正常正文和选项，accepted-final 精确读回且人物模型调用为 0；但主输出在大型辅助域尾部被截断，严格尾部人物档案回执因此完全缺失。P1 正确以 `profile_block_missing` fail-closed、零半档案并留下 durable repair receipt；同时，ticket-only P4 清理被误当成上一回合世界包屏障，使独立 P3 始终未启动。
- 最新配套预设的终检升级为 V4：完整 `<人物档案更新>` 或 `<!-- 人物档案无变化 -->` 必须紧接 `</content>`，位于论坛、选项、UpdateVariable、吐槽和状态块之前。解析器只接受该严格 post-content 插槽或旧聊天的 legacy tail；只有真正位于 EOF 的未闭合块可本地补闭合，辅助域前未闭合则拒绝，避免吞并其他域。
- P4 active consumer 现在显式区分 `worldPackage` 与 ticket-only 载荷；accepted-final 仅在同 generation 的真实世界包仍待清理时让 P3 等待，ticket-only pending 不阻断 P3。`not_completed` 仍不授予人物准入，但结构世界可与 P1 独立运行；SourceRef、operation epoch、ready-only 人物调度和旧 swipe 零写边界不变。
- runtime fingerprint 新覆盖档案回执位置判定与 P3 世界包屏障 helper；新增 post-content/legacy tail/EOF 修复、ticket-only pending、真实世界包 barrier 与运行时/manifest 版本一致回归。最终相关快速测试 8/8、唯一一次完整 Node 自动套件 613/613 均通过；实际 loaded hash 和同一聊天当前 swipe 真实复验仍待完成，十二回合正式门禁未完成。

## 2.0.0-rc.22（2026-08-20，测试仓候选）
- rc.21 在唯一测试聊天的同一首回合直接 reroll 后，正文与选项正常、玩家边界正常且人物模型调用为 0，但主模型仍没有交回人物档案块；运行时把“整份回执缺失”误判为 durable `no_candidates`，随后 P3 在 P1 唤醒前后均以零模型 stale 结束，P4 未准备。旧 swipe 保持零档案、零 Registry、零 P3/P4 消费，reroll 隔离本身通过。
- 当前票据合同改为显式二选一：回复末尾必须是完整隐藏人物档案更新，或严格尾部 `<!-- 人物档案无变化 -->`。精确 SourceRef 上存在预留票据但两种回执都缺失时，P1 现在以 `profile_block_missing`、`emptyOperations` 和 repairable 缺项收据失败，不再伪装成功；无票据旧聊天仍兼容原省略语义。
- P1→P3 安全接棒现在覆盖“初始 owner 已在 P1 检查前释放”和“P1 检查时仍 pending”两种微任务顺序；只对固定码、零写、零世界模型调用且 exact accepted SourceRef 仍当前的 stale 结果重新获取一次 owner。人物失败仍显式失败，但结构世界可独立收敛，已成功世界只收到幂等 duplicate enqueue，不会重复模型或写入。
- 单人物修复后备默认只取一个目标；runtime fingerprint 新覆盖主 semantic adapter、显式无变化判定、缺失回执判定和单人物 repair adapter。新增真实根因回归、P3 settled-before-wake 时序、显式尾部回执与 fingerprint mutation 检查。rc.22 唯一一次完整 Node 自动套件为 610/610，并通过语法、manifest/配套预设 JSON 与差异检查；当前指纹真实同聊天 reroll 仍待执行，十二回合正式门禁未完成。

## 2.0.0-rc.21（2026-08-20，测试仓候选）
- rc.20 当前指纹的真实全新聊天首回合已从 accepted-final 识别出 3 个新人物目标，但主模型没有输出任何隐藏人物档案块；P1 因 `profile_block_no_entries` 正确零写失败，人物模型调用为 0，P3/P4 随上游硬失败进入 `stale`/未准备。正文、选项和玩家自主权边界本身通过。
- 根因冻结为生成前票据提示只约束人物塑形，没有在离用户输入最近的同一载荷里把“消费票据”绑定为六段档案的必要回执；长上下文中只靠预设较早位置的通用合同不足以形成逐票闭环。
- rc.21 在既有唯一 P4 consumer 的票据尾部追加紧凑回执合同：每个正文实际消费的完整 ticketId 都必须在同一 accepted assistant 末尾、可见正文之外交回六段自然中文档案；多人物逐人完整，单个失败不得删除其他人物。没有实际新人物和既有人物变化时仍可省略。
- runtime fingerprint 现在覆盖真实 `npcDesignTicketPrompt`，并增加动态提示构造、逐票回执、可见域隔离和 fingerprint mutation 回归。rc.21 唯一一次完整 Node 自动套件为 607/607，并通过语法、manifest/配套预设 JSON 与差异检查；仍须完成推送、实际 loaded hash 和新全新聊天首回合闭环，rc.20 失败证据不能替代。

## 2.0.0-rc.20（2026-08-19，测试仓候选）
- rc.19 当前指纹的真实全新聊天首回合再次确认：正文与选项生成、人物路径 durable `no_candidates`、人物模型调用为 0，尾随宿主事件也被 Doctor 正确识别为 `ignored_start/dry_run`；但 P3 的模型前 stale 分支缺少统一 `module/zeroWrite/worldModelCalls` 收据，P1 无法安全接棒，世界仍无终态。
- 所有 P3 模型前 stale 现在使用固定隐私码和统一零写收据；P1 仍只 join 单 owner，且仅在 joined 结果明确零写、零模型、非前台抢占时，重新 fresh-read 同一个 accepted envelope 的 exact target 后取得一次新 owner。正文、swipe、generation、scope、content 或 epoch 任一变化仍零写且不重跑。
- 同一次真实回复还暴露主预设把“你感到”列为普通第二人称表达，与玩家感受自主权冲突。配套最新版预设已把它改为客观感官表达，并明确禁止替玩家下主观情绪/认知结论；不削弱第二人称、NPC 自主性或正文长度合同。
- 新增模型前 stale 收据、fresh exact target 接棒、runtime fingerprint 与预设玩家感受边界定向回归；rc.20 完整 Node 自动套件为 606/606，并通过语法、manifest/配套预设 JSON 与差异检查。当前指纹真实复验仍待执行，不能用 rc.19 失败证据替代。

## 2.0.0-rc.19（2026-08-19，测试仓候选）
- rc.18 当前指纹的真实全新聊天首回合确认：正文、选项和 accepted-final 正常，人物路径按合同形成 durable `no_candidates` 且人物模型调用为 0；但独立 P3 在 P1 写妥该证明前以零模型、零写入进入 `stale`，P1 的 `afterPending` 只 join 了这个结果，结构世界没有终态，候选判失败。
- rc.19 不恢复 rc.17 的 promise 内递归重开。P1 仍先原样 join 单一 P3 owner；仅当该 owner 明确 `stale + zeroWrite + worldModelCalls=0`、不是前台抢占、且 exact accepted target 仍当前时，才在 owner 完全释放后取得一次新 owner，并携带 durable no-candidates permit。真实语义失败、已发模型调用、swipe/chat/content/scope/epoch 漂移仍不自动重跑。
- Doctor 总终态会把上述一次安全接棒的最终 committed/readback 结果作为世界终态，不再让已被安全接棒的零写 stale 把整轮伪报失败。runtime fingerprint 已覆盖新接棒适配器。
- rc.19 的完整 Node 自动套件为 604/604，并通过语法、manifest/配套预设 JSON 与差异检查；仍须推送、重新安装并在新全新聊天完成当前指纹首回合复验，rc.18 失败证据不能替代。

## 2.0.0-rc.18（2026-08-19，测试仓候选）
- rc.17 修复后的完整 Node 套件发现 P1 的 `afterPending` 唤醒会递归重开任意非成功 P3，包括真实语义失败。rc.18 恢复成熟的单 owner join：已有 P3 的 applied/failed/stale/cancelled 结果都原样返回，不在其 promise 链内递归启动第二个世界任务。
- semantic 人物档案明确 `no_candidates` 时，P1 唤醒现在把同一 exact-source coverage proof 作为 `noActorPermit` 交给 P3；这修复了“合法省略档案块后 P3 仍缺准入证明”的根因，同时不放宽 ready-only 人物调度。
- rc.18 完整 Node 自动套件为 603/603，通过语法、manifest/配套预设 JSON 与差异检查；这些仍不是实际宿主证据。
- rc.18 仍需在当前提交、实际 loaded hash 与 runtime fingerprint 下完成新的全新聊天首回合闭环；rc.16/rc.17 证据均不能替代。

## 2.0.0-rc.17（2026-08-19，测试仓候选）
- 一次授权的真实首回合在 rc.16 发现：正文模型按“无人物变化可省略”合同没有输出人物档案块，但运行时把 32 张预留可选票误当成已消费票据，错误标记 `profile_persistence_failed`。rc.17 将“块缺失”恢复为 accepted assistant 的明确零变化、零写入语义；只有块已出现但解析、绑定、完整性或持久化失败才进入修复。
- semantic P1 的成功、无变化和真实失败现在都复用现有恢复状态持久化：无变化保存 exact-source coverage proof；真实失败保存票据批次和 retry receipt。刷新后不会把红色失败重置成“等待”。
- 独立启动的 P3 若首次在同源宿主身份窗口得到 stale，P1 终态唤醒会在原任务完全结束后、仅对同一 accepted SourceRef 安全重试一次；不同 chat/swipe/generation/content/scope 仍零写入。
- 人物页在零卡片但存在持久人物故障时显示红色普通语言摘要与修复中心指引，不再被蓝色空状态覆盖；MVU 仍是唯一档案内容权威，不用数据库或旧 Ledger 代填。
- rc.16 的唯一一次真实生成没有产生可验证的新人物档案，世界链为 stale，刷新后故障未 hydrate，因此该真实样本判失败。用户禁止第二次正文生成；rc.17 仅有定向自动回归，尚未获真实复验，更不代表十二回合正式门禁通过。

## 2.0.0-rc.16（2026-08-19，测试仓候选）
- 原“人物档案”页改为直接读取当前楼层 MVU `/人物档案/byActorId` durable 投影；数据库、世界书和旧 ActorLedger 档案不能冒充当前内容权威。
- 多人物界面改为有界滚动 accordion：默认全部折叠、同一时间最多展开一人、提供“全部折叠”，六段档案在“稳定档案/长期演化”二级分区内阅读；移动端单列并保持可触控按钮和长文换行。
- 人物名、别名、本回合新增/更新、完整回读、待修复、迁移中和持久失败均以普通语言和绿/黄/红状态显示；技术详情只展示脱敏计数，不显示 ActorId、revision、digest 或 SourceRef。
- 故障卡只在展开区调用现有单人物定向修复适配器；旧 verified profileV6 可逐人物显式迁移到 MVU，失败保持旧档案和回滚路径不变。折叠偏好只保存在浏览器本地，不进入 MVU 或 Doctor 账本。
- 唯一一次完整 Node 套件共 602 项，在修复前暴露 6 项接线/夹具失败（596 通过）；这些失败随后均已修复并由对应定向回归覆盖。依照本轮“完整套件只运行一次”的额度约束，没有伪称修复后完整套件全绿；真实首回合与十二回合正式门禁另行记录。

## 2.0.0-rc.15（2026-08-19，测试仓候选）
- 配套最新版 `01_主预设_人物万花筒_可调篇幅_IZUMI0814作者更新_ARGO1.3最小融合候选版` 在 accepted-final 尾部输出隐藏的自然中文人物档案域；新人物一次给齐六段 V6 叙事档案，已有角色只给 delta，模型不拥有技术元数据。
- accepted-final 本地适配器以完整 SourceRef、同票 ActorId 和锁定字段校验逐人物编译，复用现有 MVU selected-field WAL/CAS/readback/rollback，并把最终档案补丁合并回原主回复的 UpdateVariable，避免覆盖数据库或原有变量操作。
- MVU `/人物档案/byActorId` 成为完整档案唯一内容权威；ActorLedger 只存 profileRef/readiness 和人物行动、世界裁决字段。旧 profileV6 只读保留，显式迁移逐人物回读，失败不破坏旧副本。
- P3 与 P1 独立启动，只调度启动时已 durable-ready 的人物；坏人物不阻塞结构世界和其他人物。P4 的唯一 exact-once 插槽追加相关 ready 档案摘要、世界事件和同票输入。
- 正常链人物模型调用为零；本地修复后仍缺项时，修复中心只请求单人物缺失字段，不重跑正文或整批识别。诊断仅保存固定安全码。
- rc.15 候选当时通过完整 Node 自动套件 596/596，以及语法、manifest/配套预设 JSON 和差异检查；该旧结果不覆盖 rc.16 前端改动，也不能用于正式发布声明。

## 2.0.0-rc.14（2026-08-08）
- **2026-08-17 人物档案后续维护不再废掉同批旧档案**：真实单聊天连续验收到第4回合时，16张档案均完整且已读回，但后续合法维护同批部分人物会让6张未修改档案因历史整批投影变化失去行动资格。现保留 pending→final 当下的整批原子读回门，持久行动资格改核每个人物自己的完整档案与不可变 transaction/write-set/commit evidence；同批其他人物以后合法维护不再反向失效，当前人物或收据篡改仍拒绝。旧 v1 收据仅在能证明是同伴有效后续 commit 时兼容。
- **2026-08-17 世界长局输入真实收口**：同一新聊天连续验收到第8回合时，世界模型输入随7份完整人物档案和99条世界书增长到90,832字符，并造成第二次医生总耗时超过120秒，候选按硬协议停止。现保留完整 ActorLedger、continuity、世界书 manifest、SourceRef 与全部本地校验，只把发给世界模型的重复持久化字段改为有界自然语义投影；全部必需 Actor/Thread/Lane ID 保留，世界书正文窗口改为6K，模型总输入不超过40K且超长时不拒绝本轮。新增10人物、12事件、99条世界书、超过100K原始材料的生产提示词回归。
- **2026-08-11 测试通道同步**：原样同步已验收的 P0–P5 与“旧路径与迁移收敛”实现；P1 改为一回合多人完整档案单批生成，P2/P3 保持世界链与 MVU/数据库独立并收敛为单次世界批，P4 改为 ActionAttempt 单批提案且先持久化后裁决。
- **人物票据升级为 V3**：保留 20 个独立轴；`coreDesire/relationshipDistance` 直接使用糖糖公司成熟池，`socialMethod/decisionMethod` 直接使用当前预设选项，其余 16 轴保留项目既有池。0/1/3/6/更多人物、权威人物保护、同票绑定和票池耗尽均有本地回归。
- **旧入口收敛但历史不丢失**：普通 continuity 到 Registry 的生产写入口归零，旧 `mergeActorProfilePatches` 仅作有界零写拒绝壳；迁移 v4 确定性重试，只允许 pre-Registry 旧数据一次重建，当前 Registry、scope archive、checkpoint、receipt 与未知历史字段继续保留。
- **新增可单独导入的 P5 候选预设**：`dist/01_主预设_人物万花筒_P5同票塑形候选版.json` 及 audit 随测试仓库交付；只修改既有人物万花筒 prompt，不自动覆盖用户当前预设。
- **本轮只完成快速受控验证**：交付 JSON 3/3、同步运行模块语法 8/8、P0–P5 关键矩阵 144/144、迁移与唯一所有者矩阵 92/92；未运行浏览器、真实模型、真实数据库、SillyTavern/Tauri、构建、CI、QC 或正式发布门禁。
- **七阶段人物主权重构测试候选**：人物链收敛为生成前 `characterCreationTicket` → 已接受正文 → ActorCandidate → 聊天隔离 ActorRegistry/ActorRef → 完整 ProfileInsertCandidate → 原子保存与内容读回 → ActionAttempt → WorldAdjudicationResult。数据库、MVU、预设、缝合怪、医生与世界裁决继续各自独立。
- **完整档案才算生成成功**：新档案必须一次提交完整自然中文基线，并通过 schema、ActorRef、commitId、digest 与宿主持久层内容读回；旧 `legacy_persisted` 只读兼容，不能伪装新事务或行动就绪。人工锁、确认来源、历史版本和多人物独立提交继续保留。
- **人物尝试与世界结果彻底分权**：人物只能提交带知识、资源、能力、时间、成本、风险与可观察后果的尝试；顶层 `actionAttempts` 是唯一权威日志，世界逐项裁决后才允许落账。缺项、重复、迟到、错误 generation/branch 或玩家自主权越界全部保持待裁决且零状态写入。
- **迁移与恢复按完整作用域失败关闭**：namespace v13 / migration v3 绑定聊天、稳定角色卡、世界书语义 manifest 和 runtime 版本；全部普通 namespace 写入共用迁移守卫。旧宽松目标、坏 checkpoint、embedded attempt 和无 scope 任务只读隔离，Observation WAL 与九字段收敛证明保留已接受正文但不补造人物行动。
- **本地全量验证归零**：当前源码通过语法 13/13、JSON 93/93、阶段6定向 72/72、阶段2—5核心 125/125、全量非浏览器 429/429、完整 headless 浏览器 1/1。该结果不包含真实外部模型、数据库、SillyTavern/Tauri、构建、CI 或正式发布门，测试仓库通过不等于正式发布。
- **原创人物先塑形再落档**：医生在主回复生成前本地掷出三张原创人物骰票，每张独立覆盖价值、气质、现实目标、社交、权威、决策、说话、幽默、缺点、关系距离、日常与压力恢复十二轴；人物万花筒预设负责首次出场，正文后医生按新人物顺序把同一骰票绑定到档案。数据库/角色卡/原著、已接受正文和缝合怪明确人物设定均高于骰票，冲突轴直接作废。
- **人物档案按数据库方式填全**：正文没有明确设定的档案空列改为创作补全，不再写“未设定/未登记/待确认”；模型补全始终保留为可重写假设，确认锚点不被覆盖。填表允许宽松结构与本地修复，临时恐惧、伤势和衣着不会固化为永久人格或生理基线。
- **伪人物和死亡回生修复**：“系统播报/系统提示”等标题不再登记为NPC，旧污染会在规范化时清除；跨句的失血、呼吸/喊声衰弱与死寂可以形成终局死亡证据，死亡人物不再被世界任务重新写成逃生暗线。
- **当前场景不再被旧账本倒灌**：最新数据库scene/act作为主回复硬锚点，地点冲突的旧线程不入选；连续性渲染严格使用已登记回执的线程ID，不再出现“回执选中副本、实际注入旧主城”的双重排序串线。
- **模型二审不能抹掉本地结构警告**：一句封死人格、极端标签堆叠、类型捷径、玩家动机归因或群像整齐反应一旦被本地命中，二审模型即使返回pass也至少保留黄色提醒；明确有据的黑暗行为仍可放行，不自动洗白或回滚。
- **真实证据与当前源码强绑定**：Gemini A/B、真实模型生产链、SillyTavern 数据库和静默 Tauri 报告都必须携带同一份运行时源码指纹；本地测试指纹额外覆盖 QC 脚本，任何门禁或运行时代码变更都会使旧成功报告失效。
- **真实双通道与多槽位门禁**：目标 GemAI `gemini-3.1-pro-preview` 必须逐槽通过严格 2 槽和轻量 4 槽可解析输出；同时按当前生产配置逐槽验证严格/轻量模型。真实 API 密钥只装入 `127.0.0.1` 内存代理，浏览器仅接收一次性随机能力串，两个代理都必须删钥、停进程并关闭端口后才可通过。
- **实际生产模型复验**：GemAI `gemini-3.1-pro-preview` 保留人物多样性、抗脸谱化与主权专项门；严格 2 槽和轻量 4 槽以及三轮 24 回合生产长链改用用户当前一致的 OpenCode Go `MiniMax-M3`，两套上游指标独立计数，任何串槽、解析失败或清理失败都会阻止发布。

- **人物身份不再分裂**：新增 typed ActorRef；迁移、线程、人物分片和谱系合并明确区分姓名与内部 ID，旧污染身份只合并已有证据，不补造历史。
- **档案覆盖必须真实**：V6 的未知字段、空关系、空资源能力和空行动史各自保留明确语义；未知项不能再显示为 100%，首次行动仍需满足完整覆盖。
- **人物提出尝试，世界裁决结果**：占位/no-op 行动被拒；每项尝试必须给出窗口、成本、时间、风险、可观察后果和刺激决定，世界逐项返回裁决与行动回执后才允许写入人物或世界状态。
- **恢复与槽位健康按语义计数**：显式取消不推进 `simulatedThrough`；旧无证据取消迁回最新状态恢复；单人物拥有独立持久任务。JSON 解析/校验失败会毒化槽位并只在独立健康槽位接管一次，随后保守延期。
- **长局状态与保存放大受控**：检查点按人物账本、连续性和世界压力拆成 canonical SHA-256 内容域，通过可达引用 GC 与 2MB 字节预算共享；技术回执进入可逆列式冷层和字节受控热层。正常回合从 3 次 durable 加 1 次 debounced 收敛为 observation WAL 加最终事务 2 次 durable；force 写不再做双侧 stringify，非 force 写只复制真正变化的字段。
- **恢复与提交不再随迟到消息漂移**：人物/世界任务在 claim 时固定 materialization cut，迟到 observation 只进入未来任务；确定性 commit/checkpoint ID 保证重试幂等。最终保存失败会回滚未确认的活动账本候选，保留或从 WAL 恢复可重试任务，不宣称人物或世界已经提交。
- **注入、压力和健康口径一致**：休眠线程不注入，同线程跨代冷却，只有正文证据确认后才消费；结构化威胁与“变异/狂潮/巡视者/强大原住民”等不再绕过压力预算。面板与悬浮球统一纳入人物、世界、社交、积压、身份、档案、回执和状态预算。
- **QC 证据不再硬编码**：本地测试报告绑定当前源码指纹与实际通过数；数据库必须真实加载聊天、角色、TavernHelper 和数据库 API；真实 M3 主正文由 API 生成并进入生产事件/持久化链，发布要求三轮独立连续通过。

## 2.0.0-rc.13（2026-08-07）

- **统一后台恢复时钟**：人物与世界重试只使用本地 `observedThrough`；rc.12 遗留的混合时钟任务会迁移到当前观察回合，不再出现 `nextRetryTurn` 比观察游标更大而永久不可达。
- **积压按最新状态收敛**：一次最新状态提交会事务性覆盖同模块已包含的旧任务，既不补造旧行动，也不要求逐条重演；38 回合、155 任务的真实失败形状从 `simulatedThrough=0` 自动追平到 38，活跃积压归零。
- **失败后无需用户再发消息**：技术失败按短退避在后台自动重试，刷新后孤儿任务也会恢复；任务继续运行到完成或用户主动取消，排队中的取消会持久生效，迟到结果零写入。
- **健康状态不再假绿**：发布门同时检查传输、结构解析、事务提交、双游标收敛和活跃积压；新增 64 组确定性混合失败/刷新/重试历史，任何一组无法收敛都会阻止发布。
- **用户界面去掉内部术语**：“NPC 分片”改为“人物行动分析”，健康区显示自动重试是否排队、预计重试时间、失败模块与取消入口；诊断仍只保存隐私安全的状态元数据。
- **真实规模提高**：目标 Gemini 合成门禁提升到 74 条消息、9 人物、38 观察回合、155 个后台任务和四槽并行，不发送私人跑团、角色卡、世界书或预设全文。

## 2.0.0-rc.12（2026-08-07）

- **后台不再被医生倒计时截断**：人物、世界、社交、论坛和变量模型任务默认持续运行到完成，普通正文不等待；只有用户点击“停止当前后台任务”、切换聊天或明确重抽分支时才取消。
- **长请求遇到新回复会自动追赶最新状态**：旧候选不落账、不补造历史行动，任务改回 `pending/latest_state`，下一轮从最新双游标与账本重新生成。
- **人物档案从“显示满”改为“内容真的完整”**：九类模块按字段记录 `confirmed/designed_seed` 来源，九人物均获得不同的个人目标与计划；没有实质变化时不再每回合膨胀版本历史。
- **人物与世界结构失败按根因修复**：人物短修复提示携带完整严格 schema；世界候选自动执行一次结构修复；前后说明、围栏与首个平衡 JSON 可以安全提取，技术错误仍不改变人物语义状态。
- **修复普通物品误判装备**：只有通用“类型/品质”的消耗品、卷轴或钥匙不再触发装备槽硬错误，明确武器/装备字段仍严格校验。
- **真实规模门禁**：新增 54 条消息、9 人物、19 回合、76 个后台任务与 12 个技术失败的本地长局回放；真实 GemAI 门禁使用同量级纯合成上下文、四槽并行与人物/世界完整 schema 修复，不发送私人跑团内容。

## 2.0.0-rc.11（2026-08-07）

- **人物档案终于可直接查看**：悬浮面板新增“人物”页，显示人物选择、覆盖率、九类 V6 模块、字段来源、当前计划、锁定、手工覆盖、模块重生成和版本历史；390×844 下无横向溢出，操作按钮保持可触摸。
- **锁定与手工覆盖真正持久**：后续自动补全会重新应用手工覆盖并保留锁定字段原值；整人锁与模块锁同时阻止越权覆盖和重生成，不再出现界面保存后下一轮被种子覆盖。
- **整理世界按一次总时限收口**：人物与世界 Agent 并行共享 25—35 秒硬时限，单槽位最多接管一次；不再让多个槽位各自串行等待，第二次完整重跑改为一次短 JSON 修复。
- **重复槽位不再偷走世界整理时限**：只有端点、模型或凭据真正不同且健康的连接才计为可用 failover；同一连接重复出现在多个槽位时不再预留 10 秒。真实 GemAI 加长整链由此前 20.0 秒被医生取消修复为 21.7 秒 HTTP 200 并正常提交。
- **超长真实路径有界**：连续性账本、人物、世界书、锚点和正文按焦点压缩，完整模型输入最多 40,000 字符；新增超过 100,000 字符合成长上下文回归，覆盖诊断中约 72,900 字输入未被旧测试发现的问题。
- **全槽位失败仍可恢复**：没有本地语义变化时也会提交保守 held 收据、保留全部确认状态、黄灯提示并保留 backlog；技术失败不会再伪装成人物沉默或返回永久 stalled。
- **人物分片兼容无害格式漂移**：支持前后说明、围栏、常见 proposal/result/data 包装、无害额外字段和缺省可选数组；授权字段、人物身份、知识、证据、资源和能力越界仍保持零写入。

## 2.0.0-rc.10（2026-08-06）

- **医生不会因 API 失败永久停摆**：新增独立的观察与模拟双游标、持久 backlog、七态任务、技术回执、版本化检查点、孤儿任务恢复、立即重试和检查点恢复；模型失败不再累计为人物沉默，也不丢失已观察正文。
- **人物档案升级为 V6**：身份、人格、关系、目标、知识、资源、动态状态和行动历史按来源分层；基础/完整/完整加成人生理三档自动补全支持字段锁、人工覆盖、模块重生成与版本历史，首次正式行动前必须达到所选档案覆盖。
- **人物拥有目标，世界裁决结果**：连续性下一拍只作为刺激、机会与风险，不再写进人物当前目标；在场人物可继续行动，人物候选与世界结果分权，后台隐情在被说出、实际使用或发现前不会注入正文。
- **轻量并行而非 Agent 群聊**：本地协调器每轮至多并行两个单轮人物候选和一个世界候选，候选只写黑板，最终提交由确定性本地裁决器完成；单 Agent、单槽位或整池失败均隔离并延期恢复。
- **全局补充指令与隐私边界**：用户原文可按档案、生理、人物、世界、论坛、社交、严格和轻量等范围逐字注入；诊断只保留启用状态、范围、长度、哈希和是否注入，不保存指令原文。

## 2.0.0-rc.9（2026-08-05）

- **世界不再等玩家点名**：Actor Ledger 升至 v5，把“玩家参与后才能继续”的下一拍拆成约束，把人物可独立执行的目标留在调度队列；真实玩家名、队伍和群体会从自主人物池剔除，避免把玩家自己当 NPC 模拟。
- **语义进展成为硬门**：非等待行动必须同时给出明确意图和结构化状态变化，结算后持久保存位置、计划、资源、知识、关系、风险等世界事实。仅递减计时器不再算完整成功，宏观模型失败时会明确降级而不是伪报“已推进”。
- **抗饿死与失败轮转**：按人物重要度设置有界语义沉默上限，并用上次尝试回合做公平轮转；人物分片超时、失败或空结果也会结算失败、累计沉默并重新排队，不能永远卡在同一角色或悄悄消失。
- **消费必须有证据**：世界注入只有出现对应的具体语义证据才会标记为已消费；仅提到角色名、地点或泛化词不再误删伏笔。
- **四槽自动接管**：直接模型连接连续失败两次后短暂熔断当前槽位，并自动尝试下一个不同连接；状态与诊断记录实际接管槽位，不会把同一连接的重复配置伪装成备用线路。
- **提示词有界压缩**：连续性线程、人物、势力、环境与近期正文按到期/焦点/沉默优先截取，降低长局提示膨胀，同时保留最需要推进的幕后支线。
- **运行效果门禁**：新增12回合“瓦伦式”无人点名回放；目标 GemAI `gemini-3.1-pro-preview` 实际生成非等待人物行动并落账状态事实；真实 SillyTavern / spv8.7.4 与挂起启动、隐藏看门狗、localhost CDP 的 TauriTavern 均独立复核。

## 2.0.0-rc.8（2026-08-03）

- **逐槽位 API 路由**：严格与轻量通道不再只显示一个预设加抽象并发数；1—8 个并发槽位逐项绑定 API 预设，每槽独立串行、跨槽真实并发并轮转分流。旧配置按原并发数无损展开，删除预设会清理所有引用，“测试全部槽位”会逐项报告失败。
- **动态人格证据账本**：Actor Ledger 升至 v4；新增信息取样与典型误读、具体关系距离、自我形象与行为缝隙、习得的逆倾向能力、受压反应和恢复路径。重复的有据观察按模式渐增保留，旧 v3 账本无损迁移且未知字段保持空白。
- **去类型标签入口**：MBTI、九型、Tritype、依恋型、星座、病娇/地雷/白切黑/S-M 等类型快捷方式不会写进人物长期档案；角色卡明确提供时也只作弱偏好，训练与职责可形成逆倾向能力。
- **人物万花筒 V2**：首次出场正文最多显露三项差异，群像先覆盖全部具名持续角色；新增信息误读、压力→恢复、关系距离、自我形象缝隙与习得能力五组动态轴，并强化“删姓名、删职业、删类型”三层碰撞测试。
- **二审语义路由**：轻量人物二审新增类型标签捷径和群体整齐反应触发；明确黑暗内容仍可放行，审核目标是证据、个体阈值、决定依据、行动顺序和代价差异，不是统一温柔化。
- **目标 Gemini 实测**：使用 GemAI `gemini-3.1-pro-preview` 和四个纯合成场景，对完整 rc.7 V1 与 rc.8 V2 做低温盲评；V2 为3胜0负1平，普通首次出场、类型标签诱导和逆倾向应压胜出，多人群像持平。双方均未触发六类目标违规，原始叙事未持久化。
- **Gemini JSON 围栏兼容**：真实医生探针发现 Gemini 会把人物分片 JSON 包在完整代码围栏中；本地仅允许剥除一层完整围栏，再进入原严格字段、证据和权限校验。修复后同模型3/3调用成功，前后说明、非法 JSON、额外字段或证据越界仍然零写入。
- **版本边界**：rc.8 已完成215项自动回归、GemAI Gemini、真实 SillyTavern / spv8.7.4 与隐藏 TauriTavern 复核，并经本轮明确授权晋升默认 `main`；未创建 tag 或 GitHub Release。

## 2.0.0-rc.7（2026-08-03）

- **双通道并发池**：严格与轻量独立 API 通道均可选择同时 1—8 个请求，默认分别为2和4；同端点同密钥仍按通道分池。人物分片、关系二审、世界连续性和论坛共享轻量池，满载时按优先级排队；宿主当前连接保持串行，变量与账本写入安全门不变。
- **人物DNA持久化**：Actor Ledger 升至 v3；活世界轻量调用可用证据增量补全 NPC 的社交方式、决策方式、说话节奏、压力应对、日常习惯、盲点、现实欲望、边界与内在矛盾。既有身份不被一轮情绪覆写，未知人物和无证据候选本地拒绝。
- **反脸谱合同与二审路由**：主模型合同新增职业/阵营不等于人格、强烈情绪不覆盖长期目标、群像不可换名复用同一极端模板；人物二审可识别“一句封死人格”和高烈度通用标签堆叠，明确黑暗内容仍按证据放行。
- **人物万花筒预设**：新增独立可导入副本，包含写前人物DNA、写后换名碰撞测试，以及轻量 `<story_body>` 阅读排版和仅在真实手机聊天载体出现时启用的左右气泡；历史发送给模型时会降为纯文本，不重复携带CSS。
- **目标模型实测修正**：撤回非目标 GPT 的人物验收结论，改用与用户日志一致的 GemAI `gemini-3.1-pro-preview`。十场盲评中人物万花筒版9胜0负1平；四个明确无阴谋、暴力和隐藏创伤的普通首次出场场景为3胜0负1平。发现一次强行温情后补充“反脸谱不等于统一温柔/道歉/治愈”，三轮复测均胜且未再出现该标签。
- **Gemini 人物分片超时兼容**：真实 Gemini 医生探针发现旧默认30秒会把人物worker误判为超时；默认迁移为90秒、上限120秒，只迁移旧默认值。修复后 Gemini 三次真实合成调用均为HTTP 200，人物、势力、环境结算成功；重生成、swipe、切聊天取消和迟到零写入边界不变。
- **版本边界（历史）**：rc.7 当时先作为离线候选验收，后经用户明确授权由 PR #30 发布到默认 `main`；真实环境与同主模型 A/B 结果仍以该版本独立报告为准。

## 2.0.0-rc.6（2026-07-31）

- **Serendipity Engine**：新增医生内部独立随机通道，只生成和审计一次性偶发许可证。许可证支持 favorable/adverse/neutral/absurd、small/useful/rare/extreme、unknown/possible/revealed/irrelevant，绑定完整 chat/message/swipe/generation/branch/content 身份；旧分支、迟到结果和 `continue` 零触发零写入。
- **因果与主权双保险**：明确区分矛盾、尚未说明和低概率可能；未知来源不提前确认，外部 scene/act/then 仍为候选。好运必须先真实持续且不得自动惩罚找平衡；不利事件使用医生压力预算、最低可玩性和重大坏事响应窗口；医生不改写正文，也不读取角色卡骰池。
- **防刷与长局节制**：同一对象、同一世界状态只保留一次有效抽取，换措辞、swipe 和 regenerate 不重新开奖；长会话自动抑制密集彩蛋。设置页新增四个非技术选项，默认标准、允许极端、均衡略偏好运、自然揭示。
- **计费审计移除**：删除币种估算、价格计算、月度金额账本、提醒、硬停止与旧账不完整停用门。旧 `socialMonthlyCostLedger` 等未知设置原样保留但不读取、不迁移、不阻塞；诊断只记录调用、成功/失败、HTTP状态、尝试、耗时和上游真实 token/cache usage，缺失 usage 时保持0而不按字符估算。服务费用由用户在所选服务商处自行管理。
- **发行与预设**：版本升至 `2.0.0-rc.6`；新增 `公平导演偶发性双保险版` 与独立生成审计，未覆盖既有公平导演文件；发行包新增偶发核心且不恢复数据库最终正文桥。

## 2.0.0-rc.5（2026-07-31）

- **公平导演从单项审核升级为总压力闸**：新的可重复预设生成器保留3000～4000字、NPC自主性、软/硬行动和玩家A锁，同时增加阶段压力、同场首领上限、精英/首领后的恢复债务、最低可玩性，以及延迟、替换、互相牵制和远端留存。
- **长文不再靠威胁填字**：行动推进、后果推进、恢复推进均为合法进展；调查、补给、关系、战后处理、资源选择、势力/环境变化、既有前兆和玩家成功的持续后果成为正文长度来源。
- **外置导演最终否决**：`act/scene/then` 继续只是候选；不得覆盖角色卡约束、提前添加队友/敌人/奖励、确认未知身份，或替玩家组队、接受、回答、移动和消费。
- **骰池语义纠正**：每回合严格按当前角色卡声明重置、编号和池长；禁止跨回合续游标、越池、D4/D40临时改成D2/D5、取前N/取模/跳号，并保留短骰子审计收据。
- **原作与敌人等级门**：成就、图鉴和未来目标不再充当当前生成许可；普通/精英/首领只能单选并要求当前原作锚点；没有合适敌人时优先使用原作势力、环境、设施和机制。
- **人物账本v2**：过滤玩家/系统/环境/群体误入，未知身份揭示与异变谱系保持稳定ID；死亡、离场、休眠、唤醒进入生命周期；只有人物实际观察到的句子反向更新有限知识，隐藏内心和第三方候选不再广播。
- **人物候选完整本地门**：隔离worker显式提交旅行时间、资源消耗、使用能力和等待条件；身份、知识、时间、地点、资源、能力、因果与玩家主权全部通过后才结算。
- **人物、势力、环境三通道闭环**：三轨独立调度并共享医生自己的压力和注入预算。`due=false` 只作探索保留，不再伪装成已结算；普通环境不再统一误标为经济；世界收据覆盖计划、执行、世界结算、注入和正文消费/保留。
- **分支与重生成隔离加固**：人物和世界注入收据绑定聊天、generation serial、generation/branch和正文身份；旧swipe、旧generation和错误分支不能被新正文结算，检查点也记录完整目标身份。
- **非技术设置**：新增后台行动人数、次要人物探索槽、势力槽、环境槽、主动碰撞强度、恢复节奏、同场压力/首领上限和注入数量，并明确只控制自动医生自己的后台模拟。
- **最新真实跑团回放**：固定复现未声明D5、D20池长15却使用到第17个、至少五项首领级压力、人物分片2/2失败和`due=false`误结算；新预设九项防回归覆盖全部命中，源文件哈希保持不变。

## 2.0.0-rc.4（2026-07-31）

- **人物驱动闭环**：Actor Shard 升级为聊天级持久 Actor Ledger。重要、次要与低关注人物分别保存稳定身份、性格/欲望/底线、长期与当前目标、有限认知、位置、资源/能力、关系/承诺、隐藏情绪惯性与冲突、当前计划、上次行动、下一行动窗口、证据和版本。
- **人物轨 + 结构世界轨**：活世界不是把所有支线拟人成单一角色。势力、环境、经济、长期趋势、公共信号与因果余波拥有独立的有界调度位置；即使没有人物候选、人物分片关闭或失败，它们仍可推进、结算、冷却或自行结束，且不会被人物行动覆盖。
- **有限认知回写**：每个已接受 `<content>` 只向本地判定在场的人物登记可观察句；玩家内心、私密叙述和未传播 hidden 事实不会成为人物知识。hidden 内心仅交给该人物的隔离 worker，不进入宏观提示或公开诊断。
- **到期与公平调度**：按到期行动、主动性、机会、时限、未完成承诺、资源和沉默轮数选择有界人物；额外保留0—2个低关注探索槽。次要人物可休眠、重醒并按连续有效行动动态升格，80回合回归中无永久饿死。
- **本地行动门与世界结算**：人物提案必须通过身份、知识、时间、地点/旅行、资源、能力、证据和玩家主权校验。到期人物只能执行、改计划或给出具体未满足条件；合法后台行动结算为 hidden 独立事件，主动接触才形成可观察汇流候选。
- **主动碰撞与逐条收据**：来访、寄信、悬赏、跟踪、求助、袭击、取物以及交通、价格、舆论和环境后果可按玩家设置的0—3节奏进入世界。每条行动保留“计划→执行→世界结算→注入→正文消费/保留”收据，仍受现有1—4世界接口和总注入预算约束。
- **非阻塞与兼容**：人物模拟失败、超时、坏JSON或本地拒绝只记录中文原因并降级，不阻塞正文、数据库、变量医生、社交零写入门或世界本地时钟。旧 continuity v5 仅迁移可归属的非隐藏证据；旧聊天、重抽存档点和分支身份保持隔离。
- **界面与诊断**：设置页新增每回合行动人数、低关注探索槽和主动碰撞节奏；API v6 提供脱敏人物视图和行动收据，公开视图不返回 hidden 内心状态。
- **结构世界轨收据**：API 额外提供非人物世界轨收据；脱敏诊断只暴露轨道类型、是否到期和是否独立于人物，不导出势力名称、环境内容或剧情证据。

## 2.0.0-rc.3（已发布）

- 世界剧情接口不再固定为每回合最多1条：玩家可在“活世界与事件连续性”中设置1—4条，默认2条，实际仍允许采用0条。
- 多个事件只有在各自触发条件同时成熟，或共享时间、地点、人物、势力、资源或直接因果簇时才可共同爆发；互不相关、尚未成熟的事件不得为了凑上限强行合并。
- 主提示词中列出的事件接口数量现在与逐事件注入收据一致，并继续受优先级、时效、影响对象、触发条件和总字符预算保护。
- 当前 rc.3 发布门通过：171/171 自动测试；真实 SillyTavern 手机与桌面设置均显示默认2、范围1—4；作者 `spv8.7.4` API 116方法、公开更新和重载正常；真实 `deepseek-v4-pro` HTTP 200，失败语义仍零写入。

## 2.0.0-rc.2（2026-07-30）

- 正文 `<content>` 成为每回合唯一只读事实来源；变量医生只输出并提交经过本地校验的变量补丁，不再创建正文纠错 swipe。
- 自动变量检查收敛为每回合一个主任务，按正文指纹去重，区分自动与手动来源，并阻止旧任务晚到覆盖。
- 变量失败重试改为玩家可设 0—5 次，默认失败后重试 3 次；所有尝试仍属于同一目标绑定主任务，取消、切分支或目标过期会立即停止，最终失败零写入。
- 用户可见错误统一补充“问题是什么 / 怎么解决”。原笼统“等待正文稳定”拆分为正文、MVU 忙碌标记和变量快照状态，并明确数据库填表不参与等待；卡住的忙碌标记在正文与变量快照持续稳定后不再长期阻塞。
- 社交审计在超时、无效输出、解析失败或模型拒绝时严格零写入。
- 数据库完全独立读取正文填表；移除数据库最终正文兼容桥、医生等待和变量写入后的数据库重触发。
- 当前源码发布门通过：自动套件171/171；真实 SillyTavern 中作者 `spv8.7.4` 初次与重载均确认兼容桥不存在、API 116方法、公开更新成功、医生/数据库错误0；真实 `deepseek-v4-pro` 请求 HTTP 200，模型完成，无效社交语义零写入；390×844和1280×720界面均在视口内。
- 世界引擎新增有优先级、时效、目标、预算和消费收据的事件注入闭环。

## 2.0.0-rc.1 回归修复（未发布）

- **变量医生保留，正文改写删除**：每轮接受的 `<content>` 是唯一正文事实源。自动医生仍在正文完成后读取正文、修正前变量与规则，只输出和写入通过本地路径/类型/范围/删除权限校验的 `UpdateVariable` 补丁；任何 `HardContractCorrection`、修正版正文、选项或 swipe 输出都会被忽略。正文硬合同降为只读质量报告，不再重写、截断、补写或重新生成正文。
- **自动变量任务单一化**：同一 generation/branch/swipe 的自动变量检查只有一个主任务，以接受正文指纹去重，机制区块或状态占位符变化不再冒充正文变化。自动与手动检查分别记录来源；旧任务、迟到结果、重复事件和跨分支结果在写入前均被拒绝。
- **数据库与医生彻底解耦**：数据库按每轮接受的 `<content>` 独立填表，不等待变量医生，也不在变量写入后重新调用 `triggerUpdate()`。旧“数据库最终正文桥”保留为默认停用的升级占位，真实 `spv8.7.4` 共存探针验证它在初始与重载后均为零请求、零完成、零失败。
- **社交失败零写入**：人物关系二审遇到无效输出、解析失败、超时或 HTTP 失败时不再把所有变化映射为 revert；本轮不产生任何关系写入或删除。真实 401 探针验证 `replaceMvuData` 调用为 0，合成关系状态保持原值。
- **活世界进入可追踪闭环**：场外事件、势力、风声、环境与剧情压力按优先级、影响对象、触发条件、过期轮次和字符预算形成限量注入批次；每条收据记录进入哪个 generation、是否落入提示词、被哪轮 `<content>` 消费，或继续保留/过期。变量医生、世界引擎、数据库和论坛互不设置长时间屏障，演员分片也以正文事实和 generation/branch 身份并行运行。
- **当时的 QC 阻断记录（已由 rc.2 新证据解除）**：该次探针的授权凭据曾返回 HTTP 401，因此当时只允许保留在独立分支；rc.2 已用当前源码重新取得真实 HTTP 200、完成模型响应并验证无效社交结果零写入，本条不再代表当前发布状态。

## 2.0.0-rc.1（2026-07-27）

- **阶段10 TavernHelper 误报修复与发布解锁**：真实 fixture 的全局、预设、角色和活动脚本中均不存在 TavernDB；此前仅因加载了普通 TavernHelper 主机就报 `database.barrier_not_registered`，属于 fail-closed 误报。检测现改为要求具体证据（公开 TavernDB 全局、活动脚本名、脚本树签名，或数据库脚本直接监听 `MESSAGE_RECEIVED`）。真实酒馆自检显示“未检测到 TavernDB”；自动浏览器回归同时证明隐藏 TavernDB 脚本出现时仍会阻断，直到 `id=taverndb`、`settledOnly=true`、`terminalReceipts=true` 注册。完整161/161、Actor定点14/14和17/17回放通过，候选包已重建。
- **阶段10真实QC（修复前阻断记录）**：候选源码在真实 SillyTavern 1.18.0 中完成桌面与390×844论坛指针/全文/ARIA、44消息长局、世界书与既有脚本共存和控制台零错误复核；40层 Actor Shard 合成长局与完整161项测试通过。更新后的内存凭据运行取得19/19次HTTP 200，严格/轻量通道、主回复、Actor与连续性均有真实成功收据；1/3/5上限均现场保存并运行，但所选聊天只有1个合格离场NPC，因此每批实际只调度1个worker。Scenario Plan 5A仍未出现真实活动计划，运行中三类stale仍只具备自动回归证据。该轮曾因把 TavernHelper 主机误判为外部 TavernDB 而阻断；此结论已由上方定点修复取代。
- **阶段9宿主接线验收**：Actor Shard 已通过真实 `index.js` 浏览器运行时接入回归；默认关闭时零额外调用，启用后只在持久正文屏障 `settled` 后以独立fast lane并发，严格提案随后进入原宏观连续性模型，最终仍沿用既有完整身份、事务和下游写入门。
- **有限认知与证据闭环**：worker返回的 `knowledgeBasis` 与 `evidence` 现在也必须是所分配有限上下文的子集；伪造私人认知、新证据、额外授权/写入字段或越界来源统一拒绝。发布QC指纹同步纳入 Actor Shard 核心和类型声明，避免已测入口与实际打包文件脱节。
- **阶段9自动集成证据**：40层合成长局覆盖0/1/3/5选择、多个swipe/branch、乱序并发、worker失败/超时、regenerate/swipe/切聊天整批stale六类下游零写入、论坛/世界书/伴生字段保真、提示词canary及关闭旧路径字节等价；完整套件161/161通过。真实酒馆、真实模型、移动端视觉QC、TavernDB复核与发布仍属于独立阶段10。
- **阶段8 Actor Shard / NPC分片（独立分支，待真实QC）**：settled正文和硬审计之后增加默认关闭、每回合1—5个worker（默认2）的只提案层；隔离轻量调用、严格JSON白名单、确定性汇合、TaskLease进度/取消/超时与完整目标stale零写入进入自动回归，worker失败继续走旧宏观连续性路径。
- **用户自由提示词插槽**：世界连续性与NPC分片各提供最长6000字符的清楚标记指令槽；不做题材/NSFW过滤且无默认破限示例，只影响叙事/模拟候选，不能替代授权、证据、事务、分支、危险确认、数据库屏障或硬字段校验；诊断只导出启用状态、长度和哈希。
- **发布后真实长局硬化（独立分支，尚未晋升）**：依据65消息、48个swipe、约3.5 MiB旧档续玩证据补齐数据库握手、长期费用、结构修复、连续性来源和诊断隐私；真实环境检测到 TavernHelper/TavernDB 未注册 settled-only barrier，发布晋升按真实结果阻断，`main` 与既有候选分支保持不动。
- **数据库改为显式协议客户端**：公开 API 升为 v5；数据库必须以稳定客户端ID注册 barrier v1，承诺 `settledOnly=true` 和终态收据。每个 settled/failed/stale 都生成持久收据；failed/stale 只允许 `abandon`，任何写入确认永久拒绝。未注册时环境自检准确显示“数据库未注册 barrier 协议”。
- **人物二审历史保护（已由 rc.6 替换）**：该候选曾增加本地金额保护；rc.6 已完整移除这套逻辑，历史条目不再描述或承诺当前行为。
- **轻量模型坏结构只修一次**：HTTP成功但人物二审结构无效时，快速通道只做一次严格结构修复重试；仍无效时记录 `social.invalid_structure_after_repair`，不会把主模型变成唯一硬边界。
- **连续性不再补记失败楼层**：每个待补来源按当前 message/swipe/generation/branch 身份核对完整 barrier 历史；failed/stale 来源永久跳过并保存可检查收据，旧分支同楼记录不会覆盖当前来源。
- **诊断默认最小披露**：剧情摘要、finding/decision理由、完整提示词、原始payload和完整 User-Agent 不再导出；仅保留 verdict、数量、错误码、哈希、粗粒度平台与浏览器内核大版本。隐私 canary 对密钥、私人正文、派生剧情、完整提示词和原始载荷均要求0命中。
- **刷新与重抽身份闭环**：barrier 持久键纳入 message、swipe、内容指纹、generation 与 branch，历史记录不再按聊天+楼层覆盖；执行中重roll、刷新/重启后的 stale 与 exactly-once 均有浏览器回归。
- **旧档字段保真**：3.5 MiB、65消息、48 swipe合成演练保留未知字段、TavernDB字段、推进重roll助手V2字段和当前swipe来源身份，并完成只读回滚摘要校验。
- **阶段7迁移与回滚**：新增只读1.x升级演练、容量上限、V2 sidecar诊断和摘要校验回滚；旧聊天与未知作者字段保持可读，超限或歧义失败不留下部分迁移。
- **发布候选门正式接管结构TODO**：第17项 `RR-RELEASE-REAL-QC-OVERRIDES-SIMULATION` 升级为真实行为测试；真实SillyTavern失败、候选指纹不一致或包SHA不一致均阻断发布，且不会生成publish授权。
- **发布硬化**：性能、容量、隐私、安全、故障恢复、同主模型开/关消融和真实长局进入结构化证据门。
- **伴生脚本共存门**：数据库、推进重roll助手、骰子前端及其他既有脚本的事件/控件/存储隔离和运行时0错误成为发布硬化条件；重roll仍由消息、swipe、generation、branch和fingerprint共同判陈旧。
- **候选交付物**：新增迁移/回滚指南、发布清单、2.1未决项、阶段7回放报告、线上候选分支，以及供维护者复核的离线RC包和SHA256；维护者明确授权后，`2.0.0-rc.1` 已通过非强制快进发布到 `main`，更新前版本保留在独立备份分支。
- **新增RC使用说明**：已有安装只点击“MVU 自动医生（通用）”这一行自己的“更新”，不需要加载分支列表或导入ZIP；全新安装使用仓库地址并将分支或标签留空。同时明确日常使用、线上向前回滚和脱敏反馈步骤，并把数据库、可视化、骰子前端、缝合怪与推进重roll助手V2的共存测试写入候选说明。
- **缩边悬浮球恢复移动端可点击性**：贴边收起仍保留视觉缩进，但 `clip-path` 不再把50px按钮裁成仅15px宽；左右两侧至少保留44px真实命中宽度，390×844自动回归和真实SillyTavern边缘点击均通过。
- **真实旧档容量修订**：用户授权的更新前最终记录包含57条消息、44个swipe和3,321,711字节迁移投影；默认有界窗口由1 MiB调整为8 MiB，真实只读迁移和摘要回滚通过，超限仍零sidecar回退。
- **诊断可审阅性修订**：注入注册状态不再在最终提示词事件前误报为false；变量、关系、活世界和论坛的模型调用收据补充脱敏目标楼层索引，不保存正文或原始payload。
- **版本一致性**：`index.js`、manifest、package与lock统一为 `2.0.0-rc.1`。

- **正文稳定屏障进入生产 API v4**：主回复先持久登记 `captured`，再按 `repairing → state-committing → settled` 推进；异常和目标变化分别以 `failed/stale` 终止，不再把失败包装成 settled。
- **所有下游改为 settled-only**：活世界、数据库兼容入口、记忆和论坛只读取最终回读指纹对应的正文；修复未完成时保持阻塞，旧分支或失败屏障直接放弃且不回退旧正文。
- **新增持久幂等、恢复和任务租约**：`v2/runtime/` 提供 CAS 版本记录、生产 `PersistentIdempotencyStore`、恢复记录、进度/心跳/软取消/硬超时与迟到结果零写入门禁；阶段5宿主执行桥会收到同一持久运行时。
- **数据库事故族使用统一拒绝门**：字段长度、SQL参数化和乐观修订号同时验证并一次报告全部问题；任一失败都不会调用数据库提交。
- **阶段6真实回放自动化**：三条阶段6 fixture 升级为 `unit-active`，全矩阵报告和真实 SillyTavern QC 模板进入仓库；阶段7发布候选门仍保持结构 TODO，不成为默认失败 CI。

- **自然语言和可见控件使用同一命令总线**：新增 `v2/surface/`；注册表达、有界语义 action ID 与UI选择都生成同一个阶段4原生 DomainCommand candidate，随后统一经过目标绑定确认、阶段3 Turn Boundary、领域命令验证和 Transaction proposal 规划。
- **五类领域动作逐字段证明双源等价**：物品、装备、技能、社会与任务的规范命令、确认摘要、稳定幂等键、精确写计划、前置条件和整笔事务在两入口完全一致；缺动作槽位、证据、授权、战役配置、消息身份或分支时保持 unresolved/rejected。
- **新增可审计导演台**：设置页与悬浮工具页可以打开裁定、事务、分支、证据、迁移缺口和撤销视图；沉浸/可审计/调试模式共用同一白名单诊断投影，不显示原始自然语言、完整提示词、密钥、私人路径或证据正文。
- **危险动作确认不能绕过**：确认摘要绑定规范动作与完整目标指纹；确认前不产生可提交事务，旧目标确认不能复用；生产提交只委托可选的 `MvuAutoDoctorV2Host.executePlannedDomainTransaction`，没有宿主桥时保持只读。
- **移动端与键盘回归进入默认测试**：390×844验证44px触控目标、零横向溢出、受控展开、Tab焦点圈、Escape关闭/焦点归还与键盘触发；`RR-UI-ANDROID-EXPAND` 升级为 `unit-active`。

- **新增无宿主依赖的领域事务适配层**：`v2/domain-transaction/` 把阶段3 valid命令、完整消息指纹、active Branch、显式战役配置和阶段1领域前后值转换为带精确路径、前置条件、领域诊断与稳定幂等键的阶段2 Transaction proposal。
- **物品、装备、技能、社会和任务使用真实复合事务**：类型化药剂效果与数量、装备穿脱/换槽/背包/加成、技能多资源成本、社会轴回退、任务终态/替代/资源结算均作为单次原子提交；任一前置条件失败不产生部分写入。
- **H2结算不再提前确认事实**：类型化 cost 与 candidate Fact确认同事务提交；显式 check 只有成功且带roll证据才确认，失败保持 candidate；H3继续作为显式新分支边界而非领域写入。
- **任务终态、替代和单次结算落地**：终态任务不可复开，替代任务要求目标路径显式 absent，资源和 `settlementTransactionIds` 与任务状态同事务提交，重复幂等提交最多结算一次。
- **1.x状态使用惰性只读迁移诊断**：真实5.4.1形状的脱敏投影按访问映射并公开 `pending/mapped/unresolved/quarantined`，未知字段保留在 `extensions.legacy`，不猜槽位、数值、资源、关系轴或任务冲突。
- **阶段4回放进入默认测试**：普通善意 fixture 升级为 `unit-active`；药剂、技能、装备、普通善意和强制/自愿关系五例直接调用真实阶段4规划入口，未到阶段5—7的行为仍保持 todo。

- **新增无宿主依赖的导演层**：`v2/director/` 实现 Turn Boundary、风险召回、H0–H3 Claim Adjudicator、Fact/Knowledge 状态转换与主模型上下文合同，不接 UI、外部模型、数据库或阶段4领域写入。
- **玩家负约束成为本轮硬边界**：结构化裁决同时保留正授权、明确负约束、未选候选和已选黑暗行为；NPC与环境仍可在允许空间内自主反应，但模型不得替玩家新增移动、行动、对白、技能、资源消费、检定、语气、态度或心理。
- **风险召回与最终语义裁决分离**：正则只返回需要语义复核的风险候选，永远不产生最终接受/拒绝；缺少结构化语义依据时保持 unresolved，不猜玩家意图。
- **H2/H3 输出可执行命令合同**：H2在结果前保留 candidate，并输出显式检定或类型化代价命令；H3默认拒绝，只有显式改写授权才输出保留原分支的 `new-branch` 命令。
- **事实与知识使用独立证据门**：随机口令、论坛流言、模型提案和NPC怀疑最多建立 candidate/suspected；Fact confirmed 不会自动传播为所有角色 verified，也不会授予自己人或通行身份。
- **激活五个阶段3行为回放**：玩家不移动、合理口胡、优势口胡、改写性口胡和随机暗号已调用真实阶段3 API；错轮回复继续通过阶段2 MessageFingerprint 回归。
- **新增无宿主依赖的事务内核**：`v2/transaction/` 实现 MessageFingerprint、Branch、Transaction、幂等键、单写入队列以及 `prepare/commit/abort/rollback/stale` 状态机，只暴露最小宿主桥接接口，不接 UI 或阶段4领域写入。
- **冻结消息身份与分支边界**：身份来源按显式指纹、持久医生ID、宿主原生ID、调用方显式启用的 `send_date` 依次选择；同级冲突保持 unresolved/rejected。continue 保持当前分支，regenerate/new swipe 建立新分支并退役旧分支。
- **精确写入、回读与保守回滚**：提交前复核完整目标和活动分支，写入只面向捕获的精确目标；回读失败时仅恢复本事务触及且仍等于本事务写后值的路径，保留所有外部并发合法变化。
- **激活两个阶段2行为回放**：错轮回复会进入 `stale` 且零写入；重Roll在新分支复用语义幂等键时最多提交一次，旧分支迟到任务不能污染当前分支。
- **新增无宿主依赖的V2领域核**：`v2/domain/` 提供 ItemV2、EquipmentV2、SkillV2、Fact、Knowledge、SocialState、Quest 的纯函数归一化、验证器、公开类型声明和统一 `valid/unresolved/rejected` 结果，不接 `index.js`、模型、数据库、UI或生产写入。
- **1.x只读适配保留未知字段**：旧对象未知字段进入 `extensions.legacy`，可通过只读投影往返；歧义类型、数值、槽位、资源单位、事实证据、强制/自愿关系轴或任务状态明确标记 unresolved/quarantined，不猜造。
- **冻结槽位与技能成本适配边界**：槽位系统与资源别名只由调用方显式配置；技能文本只有在数值、单位、资源、timing 与 refundable 均唯一时才生成类型化成本。
- **激活四个领域unit回放**：药剂缺少类型化效果、技能只有显示成本、装备槽位冲突、强制/自愿关系混写已进入默认行为测试；阶段4事务集成与真实回放仍保持 todo。
- **冻结共创式跑团运行层的产品边界**：叙事优先、规则兜底；导演模式支持合理即兴和口胡，但硬边界保护玩家重大行动权、资源与事务一致性、分支隔离、变量结构和知识边界。
- **建立V2数据与事务协议**：定义 ItemV2、EquipmentV2、SkillV2、Fact、Knowledge、SocialState、Quest、Transaction、Branch、MessageFingerprint 与 TaskLease，并明确硬字段、开放叙事扩展及1.x兼容迁移原则。
- **建立脱敏真实故障回放基线**：17个最小 fixture 覆盖代行动、口胡裁定、随机暗号、错轮回复、社会关系失真、装备/药剂/技能、重Roll、正文稳定屏障、数据库、Android、看门狗和真实环境发布门。
- **阶段0不改变生产运行时**：新增测试只校验规范、schema、fixture覆盖和隐私边界；尚未实现的2.0行为保留为 `todo`，由阶段1至7按路线图逐项激活。

## v1.9.0（2026-07-26）

- **正文生成前加入人物动机与自主性合同**：当前用户明确表达的行动与目的优先；NPC怀疑必须保留为有限视角；普通照顾允许保持普通；强烈情绪保留恢复空间；历史黑暗行为不再自动证明本轮善意虚伪。明确选择的威胁、洗脑、主奴或其他黑暗行为仍按设定、机制与后果完整处理。
- **未选择的四选项不再污染后续模型**：在真实 `CHAT_COMPLETION_PROMPT_READY` 负载中，仅从旧 assistant 历史移除 `<options>/<branches>` 候选；聊天存档和前端显示不变，系统格式说明与用户实际发送的选择不受影响。变量、活世界和论坛的内部摘要也使用同一隔离文本。
- **新增风险触发的轻量人物关系二审**：关系字段变化、重大标签、玩家隐藏动机归因或强制/自愿冲突才触发；平衡模式普通回合为零调用，严格模式可审核全部关系变化。
- **二审不能用温暖文风覆盖黑暗选择**：结构化审核器只能对本轮已变化的关系路径返回 `allow/revert`，不能提出新值、改写正文或改变剧情基调。明确威胁造成的合理恐惧等有证据黑暗后果必须放行。
- **强制状态与自愿关系分离并可安全回退**：被迫服从不会自动提高好感、信任、亲密、忠诚或崇拜；被拒路径恢复到本轮前值，其他 MVU 字段保持不变，纠错块持久写入当前 swipe，并复用现有写前日志、回读、撤销和目标失效门禁。
- **增加可追溯记录**：每次审核保存来源楼层、触发原因、关系路径、理由、结果与上游返回的 token 使用量，不保存原始提示词或密钥。rc.6 已删除这一历史版本中的金额边界。
- **QC加入同模型消融**：真实验收必须以同一个主正文模型分别关闭/开启合同，DeepSeek只做二审，不能把其自身温暖基调当成修复证据；固定恶意输出回放同时证明本地隔离与关系回退确实生效。
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.9.0。

## v1.8.14（2026-07-26）

- **副本从即兴续写升级为可版本化软规划**：有边界的副本、任务场、试炼或调查区会建立一次不可覆盖的初始基线，记录主目标、完成/失败条件、原生终局冲突、可选路线、时限、赌注、阶段与收束状态；它约束世界事实，不替玩家规定行动路线。
- **允许自由修正，但所有改动必须可追溯**：目标、完成条件、最终 Boss、路线、阶段、时限与赌注均可被玩家行动、世界支线、既有设定或系统规则改变。每次修订都要登记稳定ID、来源事件、触发、作用机制、证据、字段前后值与需要保留的既有成果，初始基线不会被覆盖。
- **本地门禁阻断“为了续写而续写”**：世界因果修订只能引用更新前已存在且有实际进展/来源记录的事件；同回合临时编出的气氛、拦路怪、伏笔或隐藏阶段不能直接改写主目标、通关条件或终局威胁。模型直接重写当前规划、伪造旧版本、缺证据或前值不匹配都会被本地拒绝。
- **复杂支线仍可真正改变副本**：经过多轮推进的世界支线可以可追溯地引入比原生终局更强的敌人，也可以削弱威胁、打开捷径、取消冲突或改变通关方式；结构性修订必须明确保留此前战斗、探索、资源与阶段成果。
- **副本有明确终态**：完成条件满足后进入可收束/完成状态；`completed` 或 `failed` 的同一规划永久终止，不得用“真正的最终 Boss”复开继续刷怪，仍在发展的余波改记为新的世界事件。
- **规划追溯进入悬浮账本**：手机与桌面均可查看当前有效版本、不可覆盖基线、建立正文楼层和逐条修订历史；修订可返回对应正文楼层，触控入口保持至少 42px。
- **回归覆盖关键正反例**：新增旧账本无损迁移、合法多轮支线改写终局、同回合临时造怪被拒、结构变化必须保留旧成果、终态不可复开、正文注入与 390×844 规划面板测试。
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.14。

## v1.8.13（2026-07-25）

- **修复活世界注入在真实 SillyTavern 中被静默过滤**：`setExtensionPrompt` 的系统角色改用宿主要求的数值 `SYSTEM=0`，不再因字符串角色被转换成 `NaN` 而出现“已注册但未进入最终提示词”。浏览器回归现在按真实宿主角色筛选流程组装最终提示词，无法再用手工塞入哨兵制造假通过。
- **后台复杂度保留，主线接口改为有证据的自然汇流**：活动支线容量和每轮事件簇推进能力不降；自主事件从“到期必须新建1条”改为按因果价值建立0或1条，事件仍可在幕后出生、推进、失败、结束并沿 `causedBy` 派生后继。
- **新增动态交联账本**：每条支线可登记0—4级交联评分、人物/势力/地点/资源/时间/因果/公共信号通道、逐条证据和当前视角可观察入口。引擎每轮根据MVU主线锚点与最近正文重算，旧的“无交集”不再永久黏死；没有评分、证据和入口的模型声明会被本地门禁拒绝。
- **建立“事件→世界表面→汇流候选”传播链**：风声、势力、环境、敌情和跨类别影响可通过 `sourceThreads` 追溯来源事件，本地反向写入线程 `propagation`；不存在的事件ID会被过滤，使用公共信号汇流时必须真的存在非隐藏传播节点，否则本地会剔除虚报的 `public_signal` 通道。
- **主提示词只接收成熟小接口**：普通隐藏独立支线不再整份进入主回复。隐藏支线汇流时只提供风声、价格/供给、公告、环境异常、NPC态度或行动等可观察入口，不泄露幕后标题与真相；主回复每轮可自然采用0—1条，没有合适位置必须采用0条。
- **交联原因可直接核对**：事件卡新增成熟度、交联通道、逐条证据、当前可观察入口和传播节点；分类世界条目显示来源事件ID，打开面板即可判断一条支线为何保持幕后、为何成为候选，以及它产生了哪些世界表面。
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.13。

## v1.8.12（2026-07-25）

- **直接采用 Z 论坛式单入口整帖展开**：每个主题底部只保留一个“展开 N 条评论”；一次点击同时显示楼主完整正文和全部楼层，再次点击一起收起，不再把正文与回复拆成两个互相冲突的控件。
- **取消错误的字符阈值判断**：手机端两行摘要可能在 72 个字符以内就被截断，旧实现却不给这类帖子全文入口。现在所有主题都由同一个整帖状态解除楼主正文行数限制，不再出现“正文被截断但没有按钮”。
- **删除失控的原生 `details` 回复区**：旧回复列表的 `display:grid` 会覆盖关闭状态，造成按钮、正文和楼层可见性错位。回复区现改为显式 `hidden` 状态，并由整帖按钮唯一控制。
- **回归直接验证正文最后一个字**：浏览器测试要求展开后的正文与论坛源数据逐字相等、末尾文字拥有真实布局区域、无内部裁切，并且同一次点击显示全部回复；390×844 触控区仍不小于 42×42px。
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.12。

## v1.8.11（2026-07-25）

- **手机端全文按钮达到稳定触控尺寸**：390×844 真实 SillyTavern 验收发现旧按钮实际点击区只有约 40×15px；移动端现统一使用至少 42×42px 的触控区域，展开/收起状态逻辑保持不变。
- **论坛标题区不再横向撑破视口**：限制标题摘要网格的最小列宽，让单行省略文本在容器内正确收缩，避免 390px 窄屏出现额外横向滚动。
- **真实 SillyTavern 浮层不再被根变换移出视口**：SillyTavern 的根元素带变换且高度为零，固定定位浮层会错误地以这个根框计算底边，导致按钮可被辅助树发现、却位于屏幕外而无法用指针打开。悬浮球、世界面板和完整论坛现锚定固定的页面主体；390×844 实机式验收确认世界面板与论坛都完整留在可视区。
- **回归覆盖触控、真实根布局与溢出**：浏览器运行时测试复现 SillyTavern 的变换根和固定主体，新增浮层/论坛视口边界、按钮尺寸和论坛壳层横向溢出断言。
- **强制真实环境 QC**：新增真实 SillyTavern、真实 DeepSeek 请求、移动端指针交互、脱敏报告、本地收据和 `pre-push` 门禁；重大修复只有完整规程通过后才允许推送。
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.11。

## v1.8.10（2026-07-25）

- **论坛全文展开不再依赖 WebView 原生详情控件**：把正文折叠区从 `<details>/<summary>` 改为显式“展开全文 / 收起全文”按钮，由本地状态唯一控制预览、全文和 `aria-expanded`，避开 Android WebView 对阻止默认点击与原生 toggle 先后顺序不一致的问题。
- **展开与收起状态完全同步**：展开时隐藏两行预览并显示完整正文；收起时恢复预览并移除全文布局占位，按钮文案与无障碍状态同步更新。
- **真实窄屏回归补强**：浏览器宿主模拟同时覆盖长帖和中长度帖的展开、全文可见、预览隐藏与重新收起；核心逻辑和完整浏览器回归均通过。
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.10。

## v1.8.9（2026-07-25）

- **论坛正文不再“看得到截断、点不开全文”**：此前仅正文超过 180 个字符时才生成展开控件，但手机信息流两行预览会在远低于这个长度时截断内容。现在超过 72 个字符即生成全文折叠区，覆盖实际会在窄屏被截断的中长度帖子。
- **Android 触控展开兼容**：保留原生 `details` 语义，同时为带块状预览的 `summary` 显式处理触控/点击、`aria-expanded` 和展开状态，避免部分 Android WebView 点击摘要后无反应。
- **真实窄屏回归**：浏览器运行时新增中长度帖子展开测试，确认预览隐藏、完整正文显示且无障碍状态同步；完整浏览器回归已通过。
- **数据库只读取最终正文**：兼容 API 升级至 v2，为 ACU 提供按聊天与楼层等待的 `waitForTargetSettled` 屏障。数据库可以先等待变量修复、正文修正版 swipe 和开局资源同步全部落地，再读取当前最终正文；重 Roll、换聊天或目标被替换时旧屏障会返回 stale，不会放行旧分支。
- **交接信息不泄露正文**：稳定结果只包含聊天/楼层、稳定消息 ID、swipe、正文指纹和流程状态，不返回正文、变量值、端点或密钥。
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.9。

## v1.8.8（2026-07-25）

- **同一 API 不再互撞**：模型任务按实际端点与密钥归并连接，同连接严格单飞；排队任务按连接测试、变量、世界、其他、论坛排序。不同连接仍可并发，因此 M3 变量与 Gemini 世界可以同时跑，而同一公益站上的世界和论坛会自动错峰。
- **解析失败终于可追溯**：聊天内持久保存最近 80 条脱敏模型结果诊断，独立于最近 30 条操作日志。记录 transport/parse/validation 阶段、任务、通道、模型、耗时、排队时间、尝试序号、目标楼层、`failureKind`、JSON 根类型、UpdateVariable/JSONPatch/ContinuityState/ForumUpdate 标签完整性、输出长度和本地恢复结论；不保存原始提示词、回复、正文、状态值、端点或密钥，诊断包会一并导出。
- **缺标签的单对象补丁零调用恢复**：合法单个 JSONPatch 对象即使同时漏掉 `</JSONPatch>` 与 `</UpdateVariable>`，也会先本地包装为数组、补齐机器区块，再继续 MVU、Schema、写前与写后校验，不触发第二次模型调用。
- **重 Roll 世界时钟不再漂移**：模型给出的 `turn` 与 `lastAdvancedTurn` 不再能把账本推到未来；本地聊天楼层是唯一时间权威，重 Roll 后从当前分支的本地回合重新落账。
- **活世界改为稀疏事件簇推进**：模型只需返回发生变化的旧支线与新支线，遗漏旧支线由本地保留；`living` 每轮最多推进 3 条相关支线，`expansive` 最多 6 条，并按显式 lastTick、紧迫度和长期未推进时间选取，降低支线饿死。活跃支线上限提升至 8/12，默认输出预算提升至 12288。
- **真实浏览器回归**：新增同连接串行、不同连接并行、排队取消、单对象缺双闭合标签及诊断持久化、解析失败与连接失败分账、未来回合钳制和多支线批处理用例；完整浏览器运行时套件已执行，不允许把 Playwright 缺失当成通过。
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.8。

## v1.8.7（2026-07-25）

- **内置论坛重做为手机信息流**：删去占首屏的大标题、状态胶囊和厚卡片；改为紧凑应用栏、世界动态吧头、下划线分类标签与连续主题列表。
- **主题一眼读完**：默认只显示分类、标题、两行摘要、作者/时间、热度/回复数和一条热评预览；完整正文与全部楼层均按需展开，不再打开论坛就塞入一整串评论。
- **低频控制不干扰阅读**：自动刷新、间隔和清空仍在“刷新设置”里，打开页面默认收起；刷新始终保留在顶栏。
- **手机回归升级**：390×844 窄屏验收现在要求顶栏不超过 64px、默认主题不超过 210px、首屏至少同时可见两个主题，并覆盖热评、全文、评论展开、刷新切换、重开和聊天隔离。
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.7。

## v1.8.6（2026-07-25）

- **首轮单对象 JSONPatch 不再被丢弃**：模型返回语义明确的单个合法补丁对象时，本地零调用包装成单元素数组；对 `jsonPatch`、`patches`、`operations`、`ops` 等唯一已知包装数组同样安全提取。所有操作、路径、MVU、Schema、写前恢复和写后回读校验保持不变；多个候选数组或任意对象仍拒绝。
- **已有对象不再因冗余空容器初始化而整轮重试**：模型先对已存在对象 `insert {}`、随后又写入其子路径时，只移除这个可证明无效且冗余的空父对象操作；子补丁继续走完整校验。不会处理数组、非空对象或没有后续子写入的操作。
- **普通对象新旧字段的 `replace` / `insert` 混淆本地修正**：目标不存在且父节点是普通对象时把 `replace` 改为 `insert`；目标已存在且父节点是普通对象时反向修正。数组、根节点和父节点不存在的情况仍拒绝，不做猜测。
- **外层闭合、内层漏标签也能零调用恢复**：`</UpdateVariable>` 已存在但 `</JSONPatch>` 漏掉时，不再误判整个区块完整；只要 JSON 数组本身闭合并可解析，就在本地补齐标签后继续所有校验。
- **MVU 延迟挂载后自检会自动恢复**：初始化与切换聊天时等待 MVU；运行中首次取得完整 MVU API、展开自检面板或导出诊断包时都会重新检查，避免顶部长期保留“未检测到 MVU”的过期红灯，也避免诊断报告与实际能力互相矛盾。
- **不再把 MVU 自己的确定性联动误判为“补丁未落地”**：若模型只修改可写基础值，而 MVU 在本地解析时同步重算未被补丁触碰的实际属性、资源上限或生命状态，医生会用同一输入再做一次纯本地确定性复核；两次结果完全一致且字段仍存在时才纳入允许的解析器联动，不增加模型调用，也不放过字段丢失。写前恢复记录会同时保存这些联动路径，撤销时基础值和联动结果一起恢复。
- **不恢复三分钟重试链**：上述格式兼容完全在本地完成，不增加模型调用；自动变量诊断仍最多一次。
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.6。

## v1.8.5（2026-07-25）

- **重 Roll 不再继承旧正文账本**：修复 TauriTavern 在同一楼 regenerate 时直接替换整条消息对象并分配新稳定 ID，导致“整楼生成前”连续性/论坛快照无法匹配、旧回复事件继续被推进的问题。快照匹配改为聊天内逻辑楼层；新回复会从原始楼层基线重新建立事件与来源。
- **旧异步结果仍不能串写**：只放宽世界与论坛选择重抽基线时的身份判断；变量诊断、MVU 写入与正文刷新仍必须同时通过聊天 ID、稳定消息 ID、楼层、swipe、正文指纹和生成世代校验。
- **调用次数不再误读**：工具页与悬浮面板显示“本次生成”的变量/活世界/论坛/失败次数，并在末尾单列“聊天累计”；旧调用在新生成后迟到完成时只计入累计，不会污染新一轮统计。
- **真实宿主行为回归**：浏览器测试新增“同一索引的 AI 消息对象被整个替换、消息 ID 改变、swipe 仍为 0”的重抽场景，验证旧事件完全移除、快照不被覆盖、新来源绑定新消息，以及两次生成累计 4 次而本次仅显示 2 次。
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.5。

## v1.8.4（2026-07-24）

### 独立多 API 预设与低延迟并发
- **统一连接管理器**：设置页可编辑 OpenAI-compatible 端点、密钥和模型，从当前端点读取模型列表，并保存、载入或删除多个命名预设。每个预设完整保存端点、密钥、模型、酒馆后端转发开关和“地址原样使用”开关。
- **双通道直接选预设**：严格变量通道与轻量世界/论坛通道各自选择预设，可共用同一 API，也可分别使用格式遵从性模型与快速模型。生成请求默认不借用酒馆当前主模型或其他扩展；配置缺失或请求失败时明确失败，不静默回退。
- **兼容成熟连接策略**：支持完整 `/chat/completions` 地址、已含 `/vN` 的地址、自动补 `/v1`、原样 URL、浏览器直连和酒馆后端代发；模型列表同样支持直连或后端查询。
- **变量与活世界并发**：本地硬合同只有“正文字数不足”等非阻断项时，两条模型请求在同一回合并行发出；结构、骰子、时间等确定性硬错误仍须修复并复审后才能推进世界账本。
- **砍掉三分钟重试链**：自动变量诊断最多一次，手动检查在有效连接返回坏结构时最多两次；自动活世界最多一次，手动强制整理最多两次。纯字数不足只报告，不再为了补字数重写整篇正文。
- **提示词和预算收紧**：变量默认输出预算降为 8192，并裁剪重复 Schema、规则、初始化、历史和正文片段；活世界上下文同样限量，轻量通道可使用服务端 JSON mode。
- **浏览器回归**：新增多预设保存与通道路由、模型列表获取、严格/轻量双端点真实并发、独立鉴权、JSON mode 隔离以及不调用第三方扩展/酒馆主模型的端到端检查。

### 版本
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.4。

## v1.8.3（2026-07-24）

### 真实首回合兼容性修复
- **MVU 自动派生值不再被误判为补丁污染**：根据当前角色卡 Schema 变换和已启用 `[mvu_update]` 规则动态识别只读/自动计算字段。模型对这些字段的直接写入会在本地剥离，只提交可写输入；MVU 前端随后重算的现有派生字段允许变化，但字段被删除或无规则依据的旁路变化仍会被拒绝。
- **活世界不再被单纯字数不足停用**：正文低于写作预算仍会报告并尝试生成修正版，但不会阻断独立的活世界模型与账本。状态、骰子、时间和结构类确定性错误仍保持阻断。
- **候选场景时间正确结算**：当上一状态仍是 S0、用户 `<scene>` 提供候选时间、当前 MVU 恰好采用该候选一次时，不再误报时间漂移；缺少前后状态证据的真实冲突仍会拦截。
- **真实形态端到端回归**：新增“可写基础值修正 → 模型误写派生值被剥离 → MVU 自动重算 → 写后回读通过”浏览器测试，并验证仅剩短正文时第一回合仍实际调用活世界模型。

### 版本
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.3。

## v1.8.2（2026-07-24）

### 首回合调用恢复热修
- **同回合后处理不再误判换回复**：收到完成事件后同时观察当前 swipe 正文与 MVU 指纹；宿主仍在追加状态栏、变量解析或其他同楼后处理时继续等待，安静达到设置的“回复后等待时间”后重新绑定最终正文，再执行硬合同、变量模型和活世界流程。
- **手动检查读取执行时的最新正文**：“检查变量”和“检查硬规则”若排在旧任务后，不再冻结点击瞬间的正文；真正轮到该任务时才捕获最新楼层，避免按钮一按就以“目标回复正文已经变化”在模型调用前取消。
- **安全中止边界不放宽**：真正的新生成、切聊天、新 AI 楼层、regenerate 或新 swipe 仍立即作废旧任务；迟到的模型结果仍不能写入新目标。
- **零调用回归复现**：新增完成事件后正文/MVU 延迟落地，以及手动任务排队期间同楼正文同步两类手机 Chrome 时序测试；稳定后的首回合必须实际调用变量模型，旧自动请求不得落地。

### 版本
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.2。

## v1.8.1（2026-07-24）

### 首回合稳定性热修
- **同一楼只结算一次活世界**：`continue` 续写沿用原楼层稳定身份；同一 swipe 内容增长不再重复推进活世界时钟，也不再重复自动刷新论坛。`regenerate` 与新 swipe 仍按新分支从存档点重算。
- **坏回复不再污染世界账本**：变量审计因继续生成、目标变化或 MVU 未稳定而取消时，下游直接停止；变量/正文修正版写入后会立即复审当前 swipe，只要仍有任一硬错误，就不调用活世界模型、不推进账本。
- **短正文修正版要求加固**：检测到 `content-under-budget` 时，每次变量诊断或定向重试都必须返回达到最低纯汉字数的完整 `CorrectedContent`；提示明确排除 HTML 状态栏、思维链、选项与变量区块。
- **模型故障不再重复撞站**：连接、鉴权、超时、限流和服务错误只请求一次；只有连接成功但账本 JSON/语义无效时才允许一次定向重试。
- **状态文案改为真实原因**：模型连接成功但输出未通过账本校验时，明确显示“模型返回未通过账本校验”，不再误报“模型暂不可用”。
- **稳定消息身份加固**：稳定 ID 同时写入消息与当前 `swipe_info`，并在宿主替换 continue 消息对象时继承旧 ID，避免一楼被误识别为多条回复。
- **真实记录回归**：新增 continue 中断、硬合同阻断、部分修正版复审、连接失败单次调用、坏账本降级和手机 Chrome 稳定身份测试。

### 版本
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.1。

## v1.8.0（2026-07-24）

### Fable 审计落地
- **环境自检卡**：只读检查酒馆上下文、完整 MVU API、MVU 额外解析运行状态、活世界注入通道、上轮注入落地和医生模型连接；不会为了“检测”擅自修改其他扩展设置。
- **设置页重新分层**：状态总览与时间线常开，变量、硬合同、活世界、论坛和进阶设置默认折叠；完整事件账本只保留在悬浮面板，不再重复两份。
- **长任务阶段与软取消**：变量诊断明确显示读取 MVU、稳定等待、构建上下文、第 x/3 次模型分析、本地校验、写入回读；停止后使用 epoch 守卫丢弃迟到结果，宿主明确支持时同时发送 AbortSignal。
- **完整提示词透明**：显示最后一次真实提示词的任务、字符数和分段规模，可复制或下载完整内容；脱敏诊断包不包含正文、世界书、变量原文、提示词或密钥。
- **按聊天模型调用统计**：分别记录变量、活世界、论坛、失败和 429 次数并跨刷新保存，公益站用户可以直接核对真实调用量。

### 手机前端与可访问性
- 事件卡摘要只保留阶段与紧迫度，详细字段分为「当前 / 因果 / 传播与收束」，默认只展开当前并隐藏空值；事件进度条补齐无障碍属性。
- 论坛增加相对页次、新内容提示、加载骨架和评论者首字头像；悬浮球长按可归位，贴边状态扩大隐形触控区。
- 悬浮面板和论坛补齐焦点进入、Esc/关闭后焦点归还、焦点循环及 `aria-modal`；手机可点元素统一触控下限。
- 状态色、悬浮球渐变与背景改为酒馆主题语义色，补齐浅色主题回退；危险清空操作改为主题弹窗并显示将删除的数量。

### 通用性、诊断与工程质量
- 正文硬合同可识别 `<content>` 之外的常见叙事包装（如 `<gametxt>`、`<storytext>`、`<正文>`）；未知包装会明确安全降级，不把状态栏误算成正文。
- 活世界注入加入哨兵和最终提示词只读探针，可判断「已登记但未落入最终 prompt」；公开 API 升为带兼容检查的 v1，并补充自检、诊断、调用统计与注入状态接口。
- 修复 `npm test`/CI 实际未执行浏览器测试的问题：锁定 Playwright，CI 安装 Chromium，缺浏览器依赖时 CI 直接失败而不是静默跳过。
- 保持既有成本与安全语义：不新增全局 token 限制；只有变量分析结果损坏时最多三次尝试；正文修正版仍复用同一次变量调用并经过玩家行动守卫、写前恢复、写后回读和撤销保护。

### 版本
- manifest.json / index.js / package.json / package-lock.json 统一升级至 1.8.0。

## v1.7.2（2026-07-24）

### 新增
- **通知级别设置**：设置页新增「通知级别」下拉（全部 / 只弹警告与失败 / 静默），成功类 toast 不再强制打扰；所有状态仍完整记入操作时间线。
- **最近操作时间线**：悬浮面板「工具」页新增可折叠的操作时间线，保留最近 30 条带时间戳的状态记录（变量 / 硬合同 / 世界 / 论坛），历史状态不再被最新一句覆盖。
- **工程基建**：新增 `package.json`（`npm test` 一键回归）、MIT `LICENSE`、GitHub Actions CI（push/PR 自动跑全部测试）、版本一致性测试（manifest / index.js / package.json 三处版本必须一致）。

### 修复与增强
- **悬浮球状态光效改为 kind 聚合**：不再对状态文案做正则匹配，改用各状态显式传入的 kind；今后调整文案不会再让球体光效失灵（潜在 bug 修复）。
- **零改动修正版拦截**：模型偶尔原样返回正文却声称已修正，现在会在本地守卫阶段直接拒绝，不再生成无意义的新 swipe。
- **变量诊断"成稿纪律"提示**：system 提示新增一条指令，要求推理模型内部一遍推理完成后一次性输出全部区块，减少 32K 预算被反复重想耗尽导致的截断。

### 版本
- manifest.json / index.js / package.json 统一升级至 1.7.2。
