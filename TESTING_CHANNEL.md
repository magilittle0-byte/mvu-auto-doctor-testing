# MVU 自动医生测试通道

本仓库只用于提前试用尚未进入稳定仓库 `main` 的候选版本。每次推送都会保留明确的候选版本、提交号和已知阻塞；测试仓库通过不等于正式发布通过。

## 安装

1. 备份当前角色聊天。
2. 等待现有自动医生后台任务结束。
3. 禁用或移除稳定仓库安装的自动医生，避免两个副本同时监听同一聊天事件。
4. 在 SillyTavern 扩展安装界面使用：

   `https://github.com/magilittle0-byte/mvu-auto-doctor-testing`

5. 刷新页面，确认扩展名称显示为“MVU 自动医生（测试通道）”。

后续测试版推送后，只更新这一项并刷新页面。不要点击“全部更新”。

## 当前候选

- 版本：`2.0.0-rc.14`
- 实现基线：正式实现仓库 `8d8789a112cda2b43a3fb6f0f5ab0a7b532f73fb` 加本次重构阶段1—6的未提交产品差异；正式仓库未提交、未推送。
- 本次修复：人物发现先进入聊天隔离的 ActorRegistry 并取得稳定 ActorRef；原创人物在正文前取得同一张 `characterCreationTicket`，正文后才绑定、补全并原子提交完整 ProfileInsertCandidate。人物必须通过 schema、commit、digest 和宿主持久层内容读回才行动就绪。人物只提交 ActionAttempt，世界独立裁决时间、成本、风险和可观察后果；顶层 actionAttempts 是唯一权威日志。namespace v13 / migration v3 统一迁移守卫绑定 chat、角色卡、世界书和 runtime scope，旧坏状态只读隔离，Observation WAL 和严格九项目标负责恢复，失败、身份隔离和积压不会被蓝色忙碌状态遮住。
- 已通过：当前源码语法 `13/13`、仓库 JSON `93/93`、阶段6定向 `72/72`、阶段2—5跨阶段核心 `125/125`、全量非浏览器 `429/429`、完整纯本地 headless 浏览器 `1/1`（185.16 秒）、`git diff --check`。同步后另行执行测试仓库 JSON、语法、import 闭合和可加载回归。没有把旧真实环境报告算作本次通过。
- 未执行：按测试通道授权边界，本次没有运行真实外部模型、真实数据库、真实 SillyTavern/Tauri、构建、CI 或正式发布门禁。
- 已知阻塞：本候选仍缺当前源码对应的正式真实环境与发布门证据；首回合完整人物档案、刷新/重启读回、新对话隔离和人物尝试/世界裁决的实际效果等待用户跑团验证。
- 发布含义：可供自愿本地试用，不得视为稳定版或正式发布。

## 本次重点检查

1. 新建或切换对话后，面板不得带入上一对话的调用统计、`targetIndex`、槽位故障、人物任务或档案积压。
2. 新人物首回合应生成可直接阅读的完整档案；数据库已有有效字段原样保留，正文未设定的留白由模型合理创作；“未设定、未登记、未知”不得作为有效旧值保留，也不得逐格显示。
3. 生理档案启用时应按身体基线表的 20 个字段完整生成，不得留下整张空表。
4. 人物档案未达到行动就绪时，人物行动调用数应为零；空调度不得反向选中全部人物。
5. 模型输出可使用英文或数据库中文列名的 JSON、键值表或小标题；缺列时应先补表，再像数据库 INSERT 一样整表校验和提交，部分表不得被记为生成成功。
6. 通常只在“统一模型适配/破限提示词”填写内容并勾选“档案/生理”；世界和人物行动的独立插槽位于高级设置。
7. 原创 NPC 首次出场应有明显但不一次性倾倒的性格差异；同一人物档案必须绑定生成前的同一骰票，不得正文后重新抽一套。
8. 数据库、角色卡、原著、已接受正文或缝合怪已有明确设定时，骰票不得覆盖；只丢弃冲突轴，不应把整份人物档案清空。
9. 正文已经命中“一句话封死人格、极端标签堆叠、类型捷径或玩家动机归因”时，模型二审返回 `pass` 也应保留至少黄色提醒。
10. 人物档案只有在完整 schema、commitId、digest 和内容读回全部一致后才可行动；旧 `legacy_persisted`、半档案或身份隔离人物不得被显示为行动就绪。
11. NPC 尝试应先显示为待世界裁决；世界失败、延后、阻断或缺少完整九项目标时，不得扣资源、移动位置、写成成功历史或替玩家同意。
12. 切换聊天、角色卡、世界书版本、swipe、generation 或 branch 后，旧任务与检查点不得恢复到新作用域；迁移缺口应明确显示并可从当前已接受正文恢复观察。

## 回滚

出现异常时，先在医生面板取消后台任务并导出诊断摘要，然后禁用测试通道。重新安装或启用稳定仓库版本；不要同时启用两个版本。扩展代码切换不会主动删除聊天中的医生命名空间，但仍应以聊天备份为恢复基线。

## 反馈最小信息

- 测试仓库提交号；
- SillyTavern 或 TauriTavern 版本；
- 使用严格通道还是轻量通道、启用了几个槽位；
- 复现步骤与医生诊断摘要；
- 是否刷新或重启后仍可复现。

不要提交 API 密钥、Cookie、私人聊天全文或未去敏数据库导出。
