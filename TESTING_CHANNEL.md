# MVU 自动医生测试通道

本仓库只用于提前试用尚未进入稳定仓库 `main` 的候选版本。每次推送都会保留明确的候选版本、提交号和已知阻塞；测试仓库通过不等于正式发布通过。

## 安装

1. 备份当前角色聊天。
2. 等待现有自动医生后台任务结束。
3. 禁用或移除稳定仓库安装的自动医生，避免两个副本同时监听同一聊天事件。
4. 在 SillyTavern 扩展安装界面使用：

   `https://github.com/magilittle0-byte/mvu-auto-doctor-testing`

5. 刷新页面，确认扩展名称显示为“MVU 自动医生（测试通道）”。

后续测试版推送后，只更新这一项并刷新页面。不要点击“全部更新”。

## 当前候选

- 版本：`2.0.0-rc.14`
- 正式仓库来源提交：`215446aa2a3b564aac3984b5244251d0e54a3da7`
- 本次修复：人物档案改为“模型提供内容、医生本体负责调表、格式修复、字段归位和落库”；提示词对齐数据库的角色表、人设基线表与身体基线表；优先补完少数高价值档案，并让空的生理表在核心档案完成后继续重试。
- 已通过：本次 56/56 项人物档案、人物账本与 rc.14 根约束定向回归；脚本语法检查和差异检查通过。
- 未执行：按用户要求，本次没有运行真实 Gemini、真实数据库、SillyTavern/Tauri 或完整发布门禁。
- 已知阻塞：上一测试快照的真实环境结果不能作为本次修复的通过证据；本次人物档案实际效果等待用户跑团验证。
- 发布含义：可供自愿本地试用，不得视为稳定版或正式发布。

## 本次重点检查

1. 人物档案应优先补完素材最完整的一两名人物，不再把所有人物轮流填成半成品。
2. 身份、人设基线和行动方向应是自然可读的完整档案，不应出现程序占位词、证据编号或来源标签。
3. 生理档案启用时应按身体基线表的 20 个字段持续补全；核心档案完成后，空的生理表仍应继续重试。
4. 模型输出格式不整齐时，医生应先提取或修复结构再落库，不应因缺少多层 JSON 包装直接丢弃内容。

## 回滚

出现异常时，先在医生面板取消后台任务并导出诊断摘要，然后禁用测试通道。重新安装或启用稳定仓库版本；不要同时启用两个版本。扩展代码切换不会主动删除聊天中的医生命名空间，但仍应以聊天备份为恢复基线。

## 反馈最小信息

- 测试仓库提交号；
- SillyTavern 或 TauriTavern 版本；
- 使用严格通道还是轻量通道、启用了几个槽位；
- 复现步骤与医生诊断摘要；
- 是否刷新或重启后仍可复现。

不要提交 API 密钥、Cookie、私人聊天全文或未去敏数据库导出。
