# MVU 自动医生测试通道

本仓库只用于提前试用尚未进入稳定仓库 `main` 的候选版本。每次推送都会保留明确的候选版本、提交号和已知阻塞；测试仓库通过不等于正式发布通过。

## 安装

1. 备份当前角色聊天。
2. 等待现有自动医生后台任务结束。
3. 禁用或移除稳定仓库安装的自动医生，避免两个副本同时监听同一聊天事件。
4. 在 SillyTavern 扩展安装界面使用：

   `https://github.com/magilittle0-byte/mvu-auto-doctor-testing`

5. 刷新页面，确认扩展名称显示为“MVU 自动医生（测试通道）”。

后续测试版推送后，只更新这一项并刷新页面。不要点击“全部更新”。

## 当前候选

- 版本：`2.0.0-rc.37`
- 实现范围：MVU durable semantic profile 前移、accepted-final exact SourceRef 事务、prompt-ready assistant 副本隔离、Doctor-owned CHAT_DELETED/GROUP_CHAT_DELETED 清理、人物/势力/环境三 lane、resolved 有界压缩和 ephemeral actorOperationalState。数据库、预设、MVU、Doctor/P3/P4 仍各自独立；语义档案不双写 legacy `profileV6`。
- 候选预设：精确 IZUMI 文件 `dist/01_主预设_人物万花筒_可调篇幅_IZUMI0814作者更新_ARGO1.3最小融合候选版.json`，当前 SHA-256 为 `2BCA3FB302098212828AD37ABE9BEC9FDC020BF7439DC5602A36A702C5A85AF1`。V6 上下文隔离条目唯一启用，V4 在真实 `prompt_order` 中停用，V7 首次入聊票据合同作为最终覆盖。任何首次入聊且没有输入 MVU 档案的稳定非玩家人物都必须绑定票据；权威设定只覆盖骰轴，不允许跳过建档。
- 静态宿主证据：TavernHelper `4.8.19`；`extensions/TavernHelper/dist/index.js.map` SHA-256 为 `7989FC1B3A47978526D63FD63C9DCB5ED68B534523BD24C223FF0B82C7A1D961`。事件负载与所有权见 [`docs/2.0/MVU_RUNTIME_SOURCE_MAP.md`](docs/2.0/MVU_RUNTIME_SOURCE_MAP.md)。这不是宿主真实触发证据。
- rc.36 真实同一聊天十二回复及追加压力输入全部返回“人物档案无变化”，MVU/Registry/人物页为0人；这是正式失败，不是无人物成功。rc.37 修复“只有原创 NPC 才消费票据”的上游根因，并要求 outgoing prompt-ready 精确落地证明；缺回执、缺批次、身份不完整或未证实提示词落地都 fail-closed。
- 自动检查：直接相关回归 426/426；本候选唯一一次完整自动套件 674/674，0 fail，duration `13457.5024 ms`；语法、manifest/预设 JSON 与 diff 检查通过。
- 尚未执行：rc.37 提交/推送、真实加载指纹、重新开始的十二回合验收、恢复门、构建、CI、QC 覆盖或正式发布门禁。
- 已知边界：默认 `actorRuntimeBindings={}`，无配置时 MVU 实时运行态显示未绑定；当前宿主没有提供可用于安全自动孤儿 GC 的权威 chat-ID 枚举，因此自动 GC 关闭，仅提供预览/确认入口。首回合档案、刷新/重启、新对话隔离、三 lane 和 attempt/world separation 的真实效果等待用户安装验证。
- 发布含义：rc.37 当前尚未冻结；即使随后提交与 testing `main` 推送，也必须加载当前指纹并从一个全新聊天重新开始真实验收。它仍只是测试通道候选，不得视为稳定版或正式发布。

## 本次重点检查

1. 新建或切换对话后，面板不得带入上一对话的调用统计、`targetIndex`、槽位故障、人物任务或档案积压。
2. 新人物首回合应生成可直接阅读的完整自然六段档案；数据库仍独立填表，正文未设定的留白由模型合理创作；消费票据但缺项的人物必须保留完整 ticketId、自然姓名、可见正文锚点和已有自然段，由 Doctor 整人物隔离并定向修复。
3. `full_adult` 生理档案启用时应按六个带 key 的 physiology 字段完整生成；缺任一字段或本地合同版本不符都必须红色待修复，不得留下假绿。
4. 人物档案未达到行动就绪时，人物行动调用数应为零；空调度不得反向选中全部人物。
5. 模型只输出自然中文语义回执；不得承担 revision/digest/SourceRef/JSONPatch/数据库字段。Doctor 从 accepted assistant 原始回执提取、逐人物原子校验和提交，部分人物隔离但完整人物继续提交。
6. 通常只在“统一模型适配/破限提示词”填写内容并勾选“档案/生理”；世界和人物行动的独立插槽位于高级设置。
7. 原创 NPC 首次出场应有明显但不一次性倾倒的性格差异；同一人物档案必须绑定生成前的同一骰票，不得正文后重新抽一套。
8. 数据库、角色卡、原著、已接受正文或缝合怪已有明确设定时，骰票不得覆盖；只丢弃冲突轴，不应把整份人物档案清空。
9. 正文已经命中“一句话封死人格、极端标签堆叠、类型捷径或玩家动机归因”时，模型二审返回 `pass` 也应保留至少黄色提醒。
10. 人物档案只有在完整 schema、commitId、digest 和内容读回全部一致后才可行动；旧 `legacy_persisted`、半档案或身份隔离人物不得被显示为行动就绪。
11. NPC 尝试应先显示为待世界裁决；世界失败、延后、阻断或缺少完整九项目标时，不得扣资源、移动位置、写成成功历史或替玩家同意。
12. 切换聊天、角色卡、世界书版本、swipe、generation 或 branch 后，旧任务与检查点不得恢复到新作用域；迁移缺口应明确显示并可从当前已接受正文恢复观察。

## 回滚

出现异常时，先在医生面板取消后台任务并导出诊断摘要，然后禁用测试通道。重新安装或启用稳定仓库版本；不要同时启用两个版本。扩展代码切换不会主动删除聊天中的医生命名空间，但仍应以聊天备份为恢复基线。

## 反馈最小信息

- 测试仓库提交号；
- SillyTavern 或 TauriTavern 版本；
- 使用严格通道还是轻量通道、启用了几个槽位；
- 复现步骤与医生诊断摘要；
- 是否刷新或重启后仍可复现。

不要提交 API 密钥、Cookie、私人聊天全文或未去敏数据库导出。
