# MVU Auto Doctor 2.0.0-rc.24 正式门禁清单

候选：`2.0.0-rc.24`（当前仅测试仓；以下完整真实门禁未执行；须在当前 loaded fingerprint 下完成同一测试聊天、同一首回合消息的 reroll 闭环）

当前状态：rc.23 已实际加载并在同一测试聊天首回合 reroll，正文、选项和无变化回执均生成，但旧四选项“唯一顺序”把回执推到 options 后，严格解析器正确 fail-closed。rc.24 用终检 V5 明确统一为 `</content> → 人物回执 → options → UpdateVariable`，Doctor 票据提示和 runtime fingerprint 同步绑定；定向 275/275 与唯一一次完整自动套件 613/613 已通过，推送、重新加载和同聊天 reroll 复验仍待完成。
这不是正式验收证据。结构化报告只接受本轮代码指纹下重新取得的真实目标模型、数据库、
SillyTavern/TauriTavern 与连续十二回合证据；旧版本结果不作为本轮通过依据。正式仓和正式
`main` 保持不动，完成全部真实门禁前不得合并或声明可用。

## rc.14 运行时完整性与性能硬指标

- [x] 所有后台恢复统一使用 `observedThrough`，不再用人物 tick 或 `simulatedThrough` 计算下一重试。
- [x] runtime v1 的不可达 `nextRetryTurn` 自动迁移为当前观察回合，技术计数与人物沉默/计划继续完全分离。
- [x] 最新状态提交事务性覆盖同模块已包含的旧任务，不补造旧行动、能力、关系、资源或玩家经历。
- [x] 技术失败无需新正文即可自动重试；刷新恢复孤儿任务；用户主动取消会同时终止排队、运行和可重试任务。
- [x] 健康诊断显示自动重试是否排队和预计时间；“NPC 分片”用户术语改为“人物行动分析”。
- [x] 精确复刻38观察回合、155任务、34活跃积压、`simulatedThrough=0` 的 rc.12 失败形状，恢复后必须追平38且活跃积压为0。
- [x] 64组确定性混合失败、刷新、延迟重试历史必须全部收敛；任一不收敛即阻止报告生成。
- [x] rc.13 的80个 inline 全量 checkpoint 以确定性夹具从约45.64MB迁移为约0.62MB；迁移重复执行字节稳定，最新检查点逐字段恢复一致。
- [x] checkpoint 按 actorLedger / continuity / worldPressure 内容域共享，canonical SHA-256 校验；可达引用 GC 保护未决与失败恢复基线，单 blob 超预算显式告警且不丢数据。
- [x] 正常浏览器生产周期只进行 observation WAL + 最终事务两次 durable 保存；最终保存单次失败和持续失败均不泄漏候选事实，刷新从失败态或 WAL 恢复。
- [x] 技术回执采用可逆列式 archive + 字节受控热层；固定 cut、迟到分支、幂等提交、hash 损坏、并发 continue 与单/全保存故障均有行为回归。

## rc.12 人物主权硬指标（历史，不作为 rc.14 通过证据）

- [x] 观察与模拟双游标、持久 backlog、版本化检查点、孤儿 running 恢复和七态任务已实现。
- [x] 技术失败只进入技术回执；不会增加人物沉默、改写人格或伪造补偿性历史行动。
- [x] 首次正式行动前档案覆盖为 100%；档案支持来源、锁、覆盖、模块重生成和版本历史。
- [x] continuity 的 nextBeat/trigger 写入人物目标为 0，只作为刺激、机会与风险。
- [x] 人物只提交行动尝试，世界裁决结果；后台成果未披露前不进入正文。
- [x] 伪造玩家行动、同意、感受均为 0；隐藏能力无历史回执为 0。
- [x] 全局补充指令按所选范围逐字注入；诊断中的原文泄露为 0。
- [x] 普通正文不等待后台模型；后台任务默认持续到完成，用户可主动取消，取消与迟到结果均零写入。
- [x] 悬浮面板可逐人查看九类 V6 档案、来源、覆盖率、锁、手工覆盖、模块重生成和版本历史。
- [x] 角色卡/世界书/正文合计超过100,000字符时，活世界模型输入仍不超过40,000字符。
- [x] 人物/世界 Agent 单轮并行且失败隔离；模型请求不设医生倒计时，结构错误只执行一次短修复。
- [x] 全槽位失败时返回 degraded/held、显示黄灯并保留 backlog；永久 stalled 为0。
- [x] 本地长局回放覆盖54条消息、9人物、19回合、76任务和12次技术失败；人物历史伪造为0。

## 产品边界

- [x] `<content>` 仍是本回合事实来源；医生不改写、截断或重生成正文，也不创建纠错 swipe。
- [x] 自动变量检查每回合只有一个主任务；手动与自动分开；旧任务、迟到、错误分支和旧 swipe 零写入。
- [x] 数据库继续独立读取 `<content>`；医生不读取数据库表、不等待数据库、不调用第三方 CRUD，也未恢复 `integrations/database-final-reply-bridge.js`。
- [x] 社交审计无效、超时、解析失败与 HTTP 失败均零写入，不执行关系回滚删除。
- [x] 论坛、连续性、人物、势力与环境属于可选后台功能，不阻塞正文、数据库或关键变量结算。
- [x] 自动医生只节制自身生成和注入的压力；正文已有过量威胁仅作为外部事实观察，不聚合、复制或升级。

## 公平导演预设

- [x] 新增 `dist/01_主预设_公平导演偶发性双保险版.json` 与独立审计，原全局节奏版未覆盖。
- [x] 增加“明确矛盾/尚未说明/低概率可能”分类、“无前兆但不矛盾”入口及许可证/最终正文双保险。

- [x] 新副本为 `dist/01_主预设_公平导演全局节奏闭环版.json`，未覆盖用户原文件。
- [x] 保留 3000～4000 字长篇目标、NPC 自主性、软行动开放、硬行动审核、有限认知、重大成功持续后果、风味调侃无默认机械惩罚、骰子审计与玩家行动权。
- [x] 增加阶段/总压力预算、同场首领上限、恢复债务、最低可玩性、延迟/替换/互相牵制/远端留存。
- [x] 行动推进、后果推进、恢复推进均合法；安静回合可以推进，不用新怪、新机关或新倒计时填字。
- [x] 外部 `act/scene/then` 仍只是候选；总闸拒绝覆盖角色卡约束、提前添加队友/敌人/奖励、确认未知身份或替玩家决定组队、接受、移动和消费。
- [x] 骰池严格服从角色卡当前骰种、每回合重置与编号；拒绝跨回合游标、越界、改骰种及“取前 N”，并保留短收据。
- [x] 敌人须有原作锚点且普通/精英/首领只能取一档；成就、图鉴和未来目标不是当前生成许可。
- [x] 结构保持 213 个 prompt、181 个 prompt order、73 个启用块；启用链错位为 0；10 个条目定向修改并保留完整总闸与最终短校验。
- [x] 最新真实跑团回归 9/9 防护用例通过；复现并拦截 D5 临时改骰、15 长度骰池索引 17、五个首领标签、人物识别失败与 `due=false` 误结算。
- [x] `人物万花筒 V2` 增加信息误读、压力→恢复、关系距离、自我形象缝隙、习得逆倾向能力、首次出场带宽和群像覆盖；保留正文阅读排版与手机聊天气泡，不覆盖既有预设。

## 医生闭环

- [x] 偶发许可证使用医生独立随机通道，不读角色卡骰池；完整身份不匹配、`continue` 与关闭状态零触发零写入。
- [x] 同对象/同世界状态防刷；好运真实持续且不自动惩罚；坏运受压力上限、最低可玩性与响应窗口约束。
- [x] 本地计费估算、价格、月账本、提醒和费用停用门已删除；旧字段原样保留但不使用。
- [x] 严格与轻量独立 API 通道分别使用可调 1—8 并发池，默认 2/4；同端点同密钥也按通道隔离，用户明确选择的宿主当前连接仍固定串行。
- [x] 人物分片、关系二审、世界连续性与论坛共享轻量池；模型请求并发不绕过完整目标身份、取消、迟到零写入和持久化写入队列。

- [x] Actor Ledger v6 使用稳定 ID，保留未知身份揭示、异变谱系、生命周期、知识、资源、证据和历史回执，并把技术失败计数从人物语义状态中拆除。
- [x] MBTI、九型、Tritype、依恋型和病娇等类型捷径不会进入长期人物档案；旧 v3 账本迁移时不凭空补齐新维度。
- [x] 玩家、系统、环境、旁白与群体不会误入人物池；死亡或已解决人物不会继续调度。
- [x] 实际玩家名从当前聊天上下文动态排除；旧的玩家依赖目标迁移为约束，不会冻结人物自己的调查、准备、联络、移动或资源行动。
- [x] 非等待行动必须包含明确意图和至少一项结构化状态变化；只走计时器、只复述摘要或空泛等待不算语义成功。
- [x] worker 全失败、超时或空结果会结算失败、累计沉默并公平轮转；重要/次要/背景人物分别受有界语义沉默上限保护。
- [x] 正文只有出现具体语义证据才消费世界注入；仅提到人物名、地点或泛化词不会误删幕后支线。
- [x] 接受的 `<content>` 只反向更新人物实际观察到的信息；隐藏内心、未传播事实和第三方候选不广播。
- [x] 人物行动按候选→身份/知识/时间/地点/资源/能力/因果/玩家主权校验→结算顺序执行。
- [x] 人物、势力、环境三通道可独立推进，共享医生自己的压力与注入预算。
- [x] `due=false` 只能保留或探索，不能世界结算；收据 ID 包含完整作用域。
- [x] 收据覆盖计划→执行→世界结算→注入→正文消费/保留，并绑定聊天、消息、swipe、生成与分支身份。
- [x] 玩家设置提供后台行动人数、次要人物探索槽、势力槽、环境槽、主动碰撞强度、恢复节奏、同场压力上限、同场首领上限与注入数量，并明确只控制医生后台模拟。
- [x] Gemini Pro 真实探针发现人物分片旧默认30秒超时；默认迁移为90秒、上限120秒，只迁移旧默认值，目标身份变化仍立即取消且迟到零写入。
- [x] Gemini 人物分片若且仅若返回“完整单层 JSON 代码围栏”，本地剥除围栏后再进入原严格解析；前后说明、非法 JSON、额外字段、证据越界和越权写入仍全部拒绝。

## rc.14 自动与真实 QC

- [x] `npm.cmd test`：282/282（42 个项目测试文件）；0失败、0取消、0跳过、0 todo；由当前源码指纹生成证据，且不纳入共享工作区的无关未跟踪测试。
- [x] Phase 6 与 Phase 7 回放：17/17、17/17，本轮重新生成并通过。
- [x] 本地收敛门：155任务精确失败形状和64组混合历史全部收敛。
- [ ] 真实 SillyTavern / spv8.7.4 / TavernDB：必须确认实际 chat、character、TavernHelper 与数据库 runtime 已加载，医生/数据库/TavernHelper 错误均为 0。
- [ ] 双真实模型：GemAI `gemini-3.1-pro-preview` 完成人物多样性、抗脸谱化与主权专项 QC；OpenCode Go `MiniMax-M3` 按实际严格 2 槽、轻量 4 槽完成三轮连续、独立的生产全链指标。每轮 24 个 API 生成回合、49 条消息、24 个备用 swipe、逐回合持久写入/读回，并在生产链迁移 80 个约 45 MB 的旧式全量检查点；私人材料外发必须为 0。
- [ ] 完全隐藏 TauriTavern：桌面与 390×844 大状态、首载/重载、资源状态、控件尺寸与项目错误通过，并精确恢复测试前实读基线。
- [x] `release:build`：86 个白名单文件，最终字节数与 SHA-256 仅记录在包外 `dist/SHA256SUMS.txt`，避免自指漂移；`git diff --check`、候选包凭据扫描与四个测试端口清理通过。
- [ ] 真实证据齐备后运行 `qc:ci/record/gate`；非草稿 PR 全绿后合并 `main`。当前这些门禁会因真实报告缺失而拒绝，不能发布。

## rc.12 自动与真实 QC（历史，不作为 rc.14 通过证据）

- [x] `npm.cmd test`：285/285；0失败、0跳过、0 todo。
- [x] Phase 6 与 Phase 7 回放：17/17、17/17，本轮重新生成并通过。
- [x] 真实 SillyTavern / spv8.7.4 / TavernDB：API 116方法，公开更新、重载、人物页通过；医生/数据库/TavernHelper错误均为0。
- [x] GemAI `gemini-3.1-pro-preview`：长局+A/B共22/22逻辑调用、4槽并行、40,000字合成上下文；两个实际 `resource_invalid` 人物首答分别修复通过；完整宿主整链3/3 HTTP 200。
- [x] 完全隐藏 TauriTavern：桌面与390×844人物页通过，API v8/55方法，沙箱精确恢复到v1.8.10，窗口/端口/进程清理完成。
- [x] `release:build`、`qc:ci/install/record/gate`、diff check 与凭据扫描通过；功能 PR #35 已由全绿 CI 合并 `main`。

## rc.10 历史自动与真实 QC（不作为 rc.12 通过证据）

- [x] `npm.cmd test`：274/274，通过；0 失败、0 跳过、0 todo。
- [x] 定点回归已覆盖 v4→v5 迁移、实际玩家/群体排除、语义状态门、worker 失败轮转、错误消费拒绝、多槽自动接管与12回合无人点名幕后推进。
- [x] Phase 6 回放：17/17；Phase 7 回放：17/17。
- [x] 真实 SillyTavern 1.18.0 部署 rc.10：作者原版 spv8.7.4 两次 HTTP 200、API 116方法、公开更新返回真值对象、重载恢复；医生/数据库/TavernHelper 错误均为0，临时数据与8011已清理。
- [x] TauriTavern 2.1.1 使用挂起启动、恢复前隐藏看门狗、portable 数据根和 localhost CDP 完成 rc.10 首载/重载、54项 API、桌面与390×844布局复核；未使用桌面控制，结束后按原 SHA-256 恢复 v1.8.10 并关闭9331。
- [x] 当前并发源码的作者原版 spv8.7.4 重验使用隔离宿主，不复制设置、聊天、角色或凭据，不修改原用户数据；临时数据与8011端口均已清理。
- [x] 目标真实模型：GemAI `gemini-3.1-pro-preview` 使用纯合成数据完成3/3调用；人物产生非等待语义行动、状态事实落账、连续失败为0，势力与环境独立结算，凭据与原始正文均未持久化。
- [x] 失败探针中的凭据只在进程内存使用；代理、端口与临时数据已清空，未写入报告、日志、文件、仓库或安装包。
- [x] 后台 headless 390×844：面板完全在视口内、无横向溢出、可见控制最小高度 42px；论坛与世界标签 ARIA 状态同步。
- [x] 后台 headless 桌面视口：面板完全在视口内、无横向溢出。
- [x] 宿主仍有可归属第三方 JS-Slash-Runner 的资源错误；报告明确 `hostConsoleCleanClaimed=false`，不把它伪报为全宿主干净。
- [x] 用户提供的微信临时源目录在完成只读审计后自动失效；原始哈希、生成副本、修改审计和回归报告已保留，未宣称在收尾时重新读取原文件。

## rc.10 历史打包、提交与发布（不作为 rc.12 通过证据）

- [x] 版本在 `index.js`、`manifest.json`、`package.json` 与 lockfile 中统一为 `2.0.0-rc.10`。
- [x] 重建 `05_MVU自动医生_v2.0.0-rc.10_离线候选.zip`，共84个文件；最终字节数与 SHA-256 记录在包外 `dist/SHA256SUMS.txt`，避免把校验值写进包内清单造成自指漂移。
- [x] 建立 rc.10 QC 报告；最终报告为 `pass`，使用当前 rc.10 指纹，不沿用 rc.9 指纹冒充通过。
- [x] 推送前必须再次获取远端并确认 `main` 没有意外分叉；只允许普通非强制提交、PR与合并。
- [x] 用户已在本轮明确要求完成测试后推送更新并进入 `main`；授权范围不包含 force push、tag 或 GitHub Release。
- [x] 未创建发布 tag 或 GitHub Release。
